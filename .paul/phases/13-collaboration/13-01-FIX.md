---
phase: 13-collaboration
plan: 01-FIX
type: fix
wave: 1
depends_on: []
files_modified:
  - components/portal/task-detail-dialog.tsx
  - components/admin/task-detail-dialog.tsx
  - app/team/tasks/[taskId]/task-detail-form.tsx
autonomous: true
---

<objective>
## Goal
Fix 3 UAT issues from plan 13-01: missing DialogDescription warnings and duplicate import.

## Purpose
Address accessibility warnings (Radix UI) and code quality issue found during portal verification.

## Output
All issues resolved, zero console warnings on dialog open.

Source: 13-01-UAT.md
Priority: 0 blocker, 0 major, 2 minor, 1 cosmetic
</objective>

<context>
@.paul/STATE.md
@.paul/ROADMAP.md

**Issues being fixed:**
@.paul/phases/13-collaboration/13-01-UAT.md

**Original plan for reference:**
@.paul/phases/13-collaboration/13-01-PLAN.md
</context>

<acceptance_criteria>
## AC-FIX-1: Portal dialog has DialogDescription
Given a client opens the task detail dialog in the portal
When the dialog renders
Then no Radix UI warning about missing Description appears in the console

## AC-FIX-2: Admin dialog has DialogDescription
Given an admin opens the task detail dialog
When the dialog renders
Then no Radix UI warning about missing Description appears in the console

## AC-FIX-3: No duplicate imports
Given the team task detail form is reviewed
When examining the import statements
Then no duplicate identifiers exist
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Fix UAT-001: Add DialogDescription to portal task detail dialog</name>
  <files>components/portal/task-detail-dialog.tsx</files>
  <action>
    Import `DialogDescription` from `@/components/ui/dialog`.
    Add a `<DialogDescription>` inside `<DialogHeader>` with visually hidden text:
    `Task details and revision request form for {task.title}`
    This satisfies the Radix UI accessibility requirement without adding visible UI.
    Use `sr-only` class to keep it screen-reader-only.
  </action>
  <verify>Open portal task detail dialog — no console warning about missing Description</verify>
  <done>AC-FIX-1 satisfied</done>
</task>

<task type="auto">
  <name>Fix UAT-002: Add DialogDescription to admin task detail dialog</name>
  <files>components/admin/task-detail-dialog.tsx</files>
  <action>
    Import `DialogDescription` from `@/components/ui/dialog`.
    Add a `<DialogDescription>` inside `<DialogHeader>` with visually hidden text:
    `Task details and comments for {task.title}`
    Use `sr-only` class to keep it screen-reader-only.
  </action>
  <verify>Open admin task detail dialog — no console warning about missing Description</verify>
  <done>AC-FIX-2 satisfied</done>
</task>

<task type="auto">
  <name>Fix UAT-003: Remove duplicate Button import</name>
  <files>app/team/tasks/[taskId]/task-detail-form.tsx</files>
  <action>
    Remove the duplicate `import { Button } from '@/components/ui/button'` on line 14.
    Keep the first import on line 11.
  </action>
  <verify>TypeScript build passes, no duplicate identifier errors</verify>
  <done>AC-FIX-3 satisfied</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- Files not related to the issues
- Core functionality that passed testing

## SCOPE LIMITS
- Only fix issues from 13-01-UAT.md
- No scope creep or additional improvements
</boundaries>

<verification>
Before declaring plan complete:
- [ ] All minor issues fixed
- [ ] Cosmetic issue fixed
- [ ] No new console warnings introduced
- [ ] TypeScript build passes
</verification>

<success_criteria>
- All UAT issues from 13-01-UAT.md addressed
- Zero Radix UI warnings on dialog open
- Ready for re-verification with /paul-verify
</success_criteria>

<output>
After completion, create `.paul/phases/13-collaboration/13-01-FIX-SUMMARY.md`
</output>
