---
phase: 14-comment-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/team/tasks/actions.ts
  - components/admin/task-detail-dialog.tsx
  - app/team/tasks/[taskId]/task-detail-form.tsx
  - components/portal/task-detail-dialog.tsx
  - app/api/portal/comment/route.ts
  - components/ui/avatar.tsx
autonomous: true
---

<objective>
## Goal

Polish the comment system: add edit/delete to comments, default revision to OFF, notify admin on every comment, highlight revision notifications, show "Client" label in portal, initials-based avatar badges on all comments.

## Purpose

Phase 13 shipped the comment system but left polish items incomplete. Users need edit/delete on comments, admin notification for every comment, and visual polish (avatars, correct labels).

## Output

- Avatars component (initials-based, no DB migration needed)
- Edit/Delete comment controls on own comments
- Admin notification created for every comment (revision = highlighted)
- Portal revision toggle defaults to OFF
- Client comments labeled as "Client" not "Team"
</objective>

<context>
@.paul/PROJECT.md
@.paul/phases/13-collaboration/13-01-SUMMARY.md
@.paul/phases/13-collaboration/13-01-PLAN.md
@app/team/tasks/actions.ts
@components/admin/task-detail-dialog.tsx
@app/team/tasks/[taskId]/task-detail-form.tsx
@components/portal/task-detail-dialog.tsx
@app/api/portal/comment/route.ts
@lib/labels.ts
</context>

<acceptance_criteria>
## AC-1: User can edit/delete own comments
Given a team member or client posted a comment
When they view it in the task detail
Then they see edit and delete icons (and can invoke them successfully)

## AC-2: Default "revision requested" toggle is OFF
Given a team member or client is posting a comment
When the comment form loads
Then the "Request revision" switch/checkbox is OFF by default (not checked)

## AC-3: Every comment creates an admin notification
Given any user posts a comment
When it is submitted
Then a notification is created in the `notifications` table (team_member_id = NULL, visible to admin)

## AC-4: Revision comment notification is visually distinct
Given a client posts a revision request
When admin views their notification panel
Then revision comment notification is visually highlighted vs standard comments

## AC-5: Portal comments show "Client" label
Given a client views comments in the portal
When comments have no author (team_member_id = null)
Then they display "Client" rather than "Team"

## AC-6: Comments show initials avatar
Given any user views any comment
When comments are displayed
Then each comment shows a small circular initials avatar next to the comment
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Create initials Avatar component</name>
  <files>components/ui/avatar.tsx</files>
  <action>
    Create a simple initials-only avatar component.
    Props: `name: string` and optional `size: number` (default 28).
    Extract first 2 letters of the name, display them centered in a 28px circle.
    Use a deterministic color based on the first letter of the name: create a small palette array of 8-10 distinct colors, use name.charCodeAt(0) % colors.length to pick.
    No DB migration needed -- no profile image exists, so this is the permanent avatar system.
    Export: `Avatar` (the circle + initials) and optional `AvatarFallback`.
    Keep it minimal - this is a comment UI polish, not a full avatar system.
  </action>
  <verify>Avatar renders with initials and colored background for any provided name</verify>
  <done>AC-6 satisfied: initials avatar component created</done>
</task>

<task type="auto">
  <name>Add edit/delete comment server actions + notification on comment</name>
  <files>app/team/tasks/actions.ts</files>
  <action>
    Add three new actions to the team actions:

    1. `editCommentAction(commentId: string, content: string)`:
       - Authenticate user, verify user owns the comment (comments.team_member_id === user.id)
       - Update comment content with `update()`
       - Return discriminated union
       - Use `revalidatePath('/admin')` and `revalidatePath('/team')` so all surfaces refresh

    2. `deleteCommentAction(commentId: string)`:
       - Authenticate user, verify ownership
       - Delete the comment with the same ownership check
       - Return discriminated union
       - Revalidate both admin and team views

    3. Modify `postCommentAction` to also create notification:
       - After inserting comment, create notification for the comment:
         ```typescript
         await supabase.from('notifications').insert({
           team_member_id: null,  // admin notification
           message: isRevision 
             ? `🔴 Revision requested on task: ${task.title}` 
             : `💬 New comment on task: ${task.title}`,
           read: false,
         })
         ```
       - The `isRevision` field determines the notification emoji/prefix for visual distinction
       - Fetch task title for the notification message (use the same `task` object that `getOwnedTask` returns)
       - This handles team member comments. Portal comments will be handled separately in the API route.

    Follow the exact auth + service-role pattern from existing actions in this file.
    Avoid: creating a new notification table or schema - use the existing notifications table.
    Avoid: adding notification-specific notification types table - just use emoji in the message for visual distinction.
  </action>
  <verify>Team members can edit/delete their own comments; admin notification is inserted on comment post</verify>
  <done>AC-1 satisfied: edit/delete actions created; AC-3 satisfied: notification on every comment</done>
</task>

<task type="auto">
  <name>Add edit/delete UI to team task detail form</name>
  <files>app/team/tasks/[taskId]/task-detail-form.tsx</files>
  <action>
    Add edit and delete icons to each comment in the display, but ONLY on the comment author's own comments.
    Use `Pencil` and `Trash` icons from lucide-react (already used elsewhere in the codebase).
    Show icons on hover or inline to the right of the comment.
    
    For edit: inline textarea to replace the comment text with a save/cancel button pair.
    For delete: simple confirmation then delete via `deleteCommentAction`.
    
    After successful edit/delete, refresh comments by re-fetching from Supabase.
    
    Also change the "Request revision" checkbox default from true to false (OFF).
    `const [revisionRequested, setRevisionRequested] = useState(false)`
  </action>
  <verify>Team member can edit/delete own comments; revision toggle is OFF by default</verify>
  <done>AC-1 satisfied: edit/delete UI for team; AC-2 satisfied: default OFF</done>
</task>

<task type="auto">
  <name>Update portal comment route for notifications + label fix</name>
  <files>app/api/portal/comment/route.ts</files>
  <action>
    Add notification creation after inserting portal comment:
    ```
    await supabase.from('notifications').insert({
      team_member_id: null,
      message: isRevision 
        ? `🔴 Revision requested: ${content}` 
        : `💬 New comment: ${content}`,
      read: false,
    })
    ```
    Note: We don't have the task title in the API route easily, so use the comment content as the notification message.
    The `🔴` emoji prefix makes revision notifications visually distinct in the admin notification panel.
  </action>
  <verify>Portal comment creates admin notification with correct emoji prefix</verify>
  <done>AC-3 satisfied: portal comments also notify admin</done>
</task>

<task type="auto">
  <name>Fix portal label from "Team" to "Client" + add avatars everywhere</name>
  <files>components/portal/task-detail-dialog.tsx, components/admin/task-detail-dialog.tsx, app/team/tasks/[taskId]/task-detail-form.tsx</files>
  <action>
    In portal task detail dialog: Change the fallback from "Team" to "Client" in the comment author display:
    `{comment.team_members?.name ?? 'Client'}`

    In ALL comment displays (portal, admin, team): Add the avatar component next to each comment.
    Import Avatar from `@/components/ui/avatar`.
    Display: `<Avatar name={comment.team_members?.name ?? 'Client'} size={28} />` to the left of each comment card.
    
    Revision notification distinction in admin notification panel:
    - The existing notification display in admin uses the `message` field directly
    - Since revision notifications include `🔴 ` prefix, they'll visually pop out automatically in the notification list
    - Add amber highlight styling to notification cards that contain "Revision requested" text
  </action>
  <verify>Portal shows "Client" not "Team"; all comments show initials avatar; revision notifications are amber-highlighted</verify>
  <done>AC-4 satisfied: revision notifications distinct; AC-5 satisfied; AC-6 satisfied</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- `supabase/migrations/001-014` — existing database schema is stable
- `middleware.ts` — auth guard is not affected
- Existing server actions — do not modify non-comment related actions
- `notifications` table schema — no migration needed, just use existing schema
- No avatar/profile image DB storage — initials only, no migrations

## SCOPE LIMITS
- Edit/delete only available on your own comments, not others'
- No bulk delete or edit-all
- No notification preferences - every comment notifies admin
- Avatars are initials only — no image upload
</boundaries>

<verification>
- Team member posts comment → admin notification appears (💬 prefix)
- Client posts revision request → admin notification appears with 🔴 prefix
- Team member edits own comment → content updates, no error
- Team member deletes own comment → comment removed from all surfaces
- Portal comments show "Client" label for unauthenticated comments
- All comments show initials avatar (colored circle with initials)
- Revision notification notification has amber highlight in admin notification panel
</verification>

<success_criteria>
- Edit/delete comment works for comment author
- Revision toggle defaults to OFF
- Every comment (team + portal) creates admin notification
- Revision notification is visually distinct (🔴 prefix + emoji makes it distinct + amber highlight)
- Portal comments labeled "Client" not "Team"
- Comments show initials avatars
</success_criteria>

<output>
After completion, create summary at `.paul/phases/14-comment-polish/14-01-SUMMARY.md`
</output>
