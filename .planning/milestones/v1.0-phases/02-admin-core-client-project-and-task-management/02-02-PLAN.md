---
phase: 02-admin-core-client-project-and-task-management
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/admin/clients/[clientId]/page.tsx
  - app/admin/clients/[clientId]/actions.ts
autonomous: true
requirements:
  - ADMIN-03
  - ADMIN-04
must_haves:
  truths:
    - "Admin can view a client's project list"
    - "Admin can create a project (name, month, year, status) under any client"
  artifacts:
    - path: "app/admin/clients/[clientId]/page.tsx"
      provides: "Project list for a specific client with create form"
      min_lines: 40
    - path: "app/admin/clients/[clientId]/actions.ts"
      provides: "Project create/delete server actions"
      min_lines: 20
  key_links:
    - from: "Project create action"
      to: "projects table (name, month, year, status, client_id)"
      via: "server action with zod validation"
      pattern: "from\\('projects'\\).*insert"
    - from: "Project list"
      to: "clients table"
      via: "client lookup by ID for breadcrumb"
      pattern: "WHERE clients.id"
---

<objective>
Build project CRUD under a client — project list page and create/delete actions

Purpose: Admin can create monthly content projects under each client
Output: Client detail page showing projects, project create/delete actions
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-admin-core-client-project-and-task-management/02-CONTEXT.md
@.planning/phases/02-admin-core-client-project-and-task-management/02-RESEARCH.md
@.planning/phases/01-foundation-database-auth-and-security/01-CONTEXT.md
@supabase/migrations/001_initial_schema.sql
@lib/supabase/server.ts
@lib/utils.ts
</execution_context>

<context>
## Key Decisions (from CONTEXT.md)
- ADMIN-03: Admin views a client's project list via nested route
- ADMIN-04: Project has name, month, year, status
- Plain HTML forms for simple CRUD (no react-hook-form needed for 4-field form)
- Server actions with revalidatePath pattern from Phase 1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create client detail page showing project list</name>
  <files>app/admin/clients/[clientId]/page.tsx</files>
  <action>
    Create `app/admin/clients/[clientId]/page.tsx` (RSC server component):

    1. Accept params: `{ params: Promise<{ clientId: string }> }`. Destructure with `await params`.
    2. Import `createClient` from `@/lib/supabase/server`.
    3. Fetch the client by ID for the breadcrumb/header.
    4. Fetch projects for this client: `SELECT * FROM projects WHERE client_id = clientId ORDER BY year DESC, month DESC`.
    5. Display breadcrumb: "Clients > {client.name}" with back link to `/admin/clients`.
    6. Display projects as a list/table: columns for name, month name (use `new Date(0, month - 1).toLocaleString('default', { month: 'long' })`), year, status badge, actions.
    7. Show "Add Project" button that opens a Dialog with project creation form.
    8. If no projects exist, show empty state: "No projects yet. Create the first one."
  </action>
  <read_first>
    - supabase/migrations/001_initial_schema.sql (projects table schema)
    - lib/supabase/server.ts (Supabase server client)
    - app/admin/clients/page.tsx (parent page pattern reference)
  </read_first>
  <acceptance_criteria>
    - app/admin/clients/[clientId]/page.tsx exists with default export
    - File queries `clients` table by id and `projects` table by client_id
    - Page contains month and year display for each project
    - Page uses Badge component for status display
    - Page contains "Add Project" button triggering a Dialog
    - Page shows breadcrumb with link back to /admin/clients
    - Projects sorted with `.order('year', { ascending: false }).order('month', { ascending: false })`
  </acceptance_criteria>
  <verify>
    <automated>
      test -f app/admin/clients/\[clientId\]/page.tsx && echo "PAGE_EXISTS" || echo "MISSING"
      grep -cq "createProjectAction" app/admin/clients/\[clientId\]/actions.ts 2>/dev/null && echo "ACTION_EXISTS" || echo "MISSING"
    </automated>
  </verify>
  <done>Client detail page shows projects with name, month, year, status badge. Add Project Dialog with Form calls createProjectAction. Back link to /admin/clients present.</done>
</task>

<task type="auto">
  <name>Task 2: Create project server actions (create + delete)</name>
  <files>app/admin/clients/[clientId]/actions.ts</files>
  <action>
    Create `app/admin/clients/[clientId]/actions.ts` with 'use server':

    1. **createProjectAction(projectId: string is NOT right — client ID)**:
       Actually accept `(clientId: string, formData: FormData)` so the route knows which client.
       - Validate: name (string, required, min 2 chars), month (int 1-12), year (int, last 3 years to next 3 years), status (enum: 'active', 'paused', 'done', default 'active').
       - Insert into projects table with client_id.
       - `revalidatePath(`/admin/clients/${clientId}`)`
       - `revalidatePath('/admin')` (dashboard shows active project count)

    2. **deleteProjectAction(projectId: string, clientId: string)**:
       - Delete from projects where id = projectId.
       - `revalidatePath(`/admin/clients/${clientId}`)`
       - `revalidatePath('/admin')`
  </action>
  <read_first>
    - supabase/migrations/001_initial_schema.sql (projects table columns and types)
    - lib/supabase/server.ts (Supabase server client)
    - app/admin/clients/actions.ts (existing action patterns for consistency)
  </read_first>
  <acceptance_criteria>
    - app/admin/clients/[clientId]/actions.ts exists with 'use server'
    - File exports `createProjectAction` function accepting (clientId, formData)
    - File exports `deleteProjectAction` function accepting (projectId, clientId)
    - createProjectAction validates month is 1-12
    - createProjectAction inserts into projects table with client_id
    - Both actions call revalidatePath with template literal paths
    - actions.ts contains `'active'` as default status value
  </acceptance_criteria>
  <verify>
    <automated>
      grep -q "'use server'" app/admin/clients/\[clientId\]/actions.ts 2>/dev/null && echo "SERVER_ACTION" || echo "MISSING"
      grep -cq "createProjectAction\|deleteProjectAction" app/admin/clients/\[clientId\]/actions.ts 2>/dev/null && echo "ACTIONS_OK" || echo "MISSING"
    </automated>
  </verify>
  <done>Create and delete project actions exist with zod validation, proper inserts/deletes, and revalidatePath calls</done>
</task>

</tasks>

<verification>
- app/admin/clients/[clientId]/page.tsx queries client by ID and projects by client_id
- Projects display with name, month, year, status badge
- Project creation uses Dialog with name, month, year, status form
- createProjectAction and deleteProjectAction exist with proper revalidatePath calls
- Back link to /admin/clients present
- Error handling via ?error= query param pattern
</verification>

<success_criteria>
- Client detail page shows all projects for that client with month/year (ADMIN-03)
- Admin can create project with name, month, year, status (ADMIN-04)
- Project delete works with revalidation
</success_criteria>
