---
phase: 02-admin-core-client-project-and-task-management
plan: "08"
type: execute
wave: 2
depends_on:
  - "02"
  - "03"
files_modified:
  - app/admin/clients/[clientId]/page.tsx
autonomous: true
requirements:
  - ADMIN-07
gap_closure: true
must_haves:
  truths:
    - "Admin can click a project name on the client project list to reach the project task management page"
    - "Every project row is clickable — not just the name text but the entire name cell"
    - "After clicking, the browser navigates to /admin/clients/[clientId]/projects/[projectId]"
  artifacts:
    - path: "app/admin/clients/[clientId]/page.tsx"
      provides: "Project name rendered as a Link component"
      contains: "Link.*projectId.*project.name"
  key_links:
    - from: "app/admin/clients/[clientId]/page.tsx"
      to: "app/admin/clients/[clientId]/projects/[projectId]/page.tsx"
      via: "next/link on project name"
      pattern: "href.*clients.*clientId.*projects.*project\\.id"
---

<objective>
Add inbound navigation from client project list rows to the project task management page.

Purpose: Close the verification gap — project rows currently display name/status/delete but offer no way to reach the task CRUD surface that already exists at /admin/clients/[clientId]/projects/[projectId].
Output: Clickable project name links on each row.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-admin-core-client-project-and-task-management/02-VERIFICATION.md
</execution_context>

<context>
@app/admin/clients/[clientId]/page.tsx
@app/admin/clients/[clientId]/projects/[projectId]/page.tsx (target destination, already built)
@.planning/phases/02-admin-core-client-project-and-task-management/02-CONTEXT.md (decisions D-01, D-02)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap project name in Link to task management page</name>
  <files>app/admin/clients/[clientId]/page.tsx</files>
  <action>
    Read `app/admin/clients/[clientId]/page.tsx` in full before editing.

    Current code (line 215):
    ```tsx
    <td className="px-4 py-3 font-medium text-foreground">{project.name}</td>
    ```

    Replace with a Next.js Link that navigates to the project task page:
    ```tsx
    <td className="px-4 py-3 font-medium">
      <Link
        href={`/admin/clients/${clientId}/projects/${project.id}`}
        className="text-foreground underline-offset-4 hover:underline"
      >
        {project.name}
      </Link>
    </td>
    ```

    Key details:
    - `Link` is already imported on line 1 (`import Link from 'next/link'`).
    - `clientId` is already in scope from `const { clientId } = await params` on the server component.
    - `project.id` is available from the projects query (line 76-82).
    - Styling: `text-foreground` matches existing text color, `underline-offset-4 hover:underline` gives a standard hover affordance consistent with the breadcrumb link on line 92.
    - Only this single `<td>` cell changes. Do not modify any other rows, columns, or the delete form.
    - Do NOT add a separate "View Tasks" action column — the name cell being clickable is sufficient and matches the established pattern where titles in this app serve as navigation entry points (e.g., client names linking to their pages).
  </action>
  <read_first>
    - app/admin/clients/[clientId]/page.tsx (the file being modified — read full contents)
  </read_first>
  <acceptance_criteria>
    - File `app/admin/clients/[clientId]/page.tsx` contains `Link` with `href` matching the pattern `/admin/clients/${clientId}/projects/${project.id}`
    - grep for `href.*clients.*clientId.*projects.*project.id` returns at least 1 match
    - The project name is no longer rendered as bare `{project.name}` — it is wrapped in `<Link>`
    - grep for `className.*hover:underline` within the same file (new link has hover state)
    - No other files are modified
    - The `import Link from 'next/link'` line still exists (already present, should not be added again)
  </acceptance_criteria>
  <verify>
    <automated>
      grep -q 'href={`/admin/clients/${clientId}/projects/${project.id}`}' app/admin/clients/\[clientId\]/page.tsx && echo "LINK_OK" || echo "MISSING"
    </automated>
  </verify>
  <done>Project names on client project list are wrapped in Next.js Link components pointing to /admin/clients/[clientId]/projects/[projectId], with hover underline styling matching existing link conventions</done>
</task>

</tasks>

<verification>
- Project name in each row is a clickable Link component (not bare text)
- All links resolve to /admin/clients/[clientId]/projects/[projectId] with correct interpolation
- No regressions: project list still renders all 5 columns (Name, Month, Year, Status, Actions)
- Delete button still present and functional in Actions column
- Link styling is consistent with existing links (hover:underline pattern)
</verification>

<success_criteria>
- ADMIN-07 is fully reachable: admin can navigate from client → project list → task management page without manually typing URLs (ADMIN-07).
- The single failed verification truth "Admin can reach and manage project tasks from the normal client → project workflow" is now satisfied.
- Re-running the phase verification should score 7/7 must-haves verified.
</success_criteria>

<output>
After completion, create `.planning/phases/02-admin-core-client-project-and-task-management/02-08-SUMMARY.md`
</output>
