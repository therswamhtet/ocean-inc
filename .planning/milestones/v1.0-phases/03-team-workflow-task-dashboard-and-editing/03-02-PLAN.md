---
phase: 03-team-workflow-task-dashboard-and-editing
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - app/team/tasks/actions.ts
  - app/team/tasks/[taskId]/page.tsx
  - app/team/tasks/[taskId]/task-detail-form.tsx
autonomous: true
requirements:
  - TEAM-03
  - TEAM-04
  - TEAM-05
  - TEAM-08
must_haves:
  truths:
    - "Team members can open a dedicated task page for one of their assigned tasks (per D-02)"
    - "The task page shows read-only task context and independently editable caption, design file, and status controls (per D-03/D-04)"
    - "Caption copy and design-file download affordances are present"
  artifacts:
    - path: "app/team/tasks/[taskId]/page.tsx"
      provides: "Team task detail page"
      min_lines: 70
    - path: "app/team/tasks/[taskId]/task-detail-form.tsx"
      provides: "Editable team task form"
      min_lines: 120
    - path: "app/team/tasks/actions.ts"
      provides: "Team-scoped task mutation actions"
      contains: "updateTeamTask"
  key_links:
    - from: "app/team/tasks/[taskId]/page.tsx"
      to: "app/team/tasks/[taskId]/task-detail-form.tsx"
      via: "server-page to client-form boundary"
      pattern: "TaskDetailForm"
    - from: "app/team/tasks/[taskId]/task-detail-form.tsx"
      to: "app/team/tasks/actions.ts"
      via: "caption/status/file actions"
      pattern: "from '@/app/team/tasks/actions'"
---

<objective>
Build the dedicated team task detail experience for reviewing task context and editing assigned work.

Purpose: Give team members a focused route for updating their own task deliverables without exposing admin-only controls.
Output: Team task detail page, client form, and team-only mutation actions.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-team-workflow-task-dashboard-and-editing/03-CONTEXT.md
@.planning/phases/03-team-workflow-task-dashboard-and-editing/03-RESEARCH.md
@app/admin/clients/[clientId]/projects/[projectId]/tasks/[taskId]/page.tsx
@app/admin/clients/[clientId]/projects/[projectId]/tasks/[taskId]/task-edit-form.tsx
@components/admin/copy-button.tsx
@components/admin/design-file-uploader.tsx
@components/admin/design-file-downloader.tsx
@lib/utils.ts

<interfaces>
From `components/admin/copy-button.tsx`:
```ts
export default function CopyButton({ text, label = 'Copy', className }: { text: string; label?: string; className?: string })
```

From `components/admin/design-file-uploader.tsx`:
```ts
export function DesignFileUploader({ projectId, onUploadComplete, taskId }: { projectId: string; onUploadComplete: (path: string) => void; taskId?: string | null })
```

From `components/admin/design-file-downloader.tsx`:
```ts
export default function DesignFileDownloader({ filePath, fileName }: { filePath: string; fileName: string })
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create team-scoped task query and mutation actions</name>
  <files>app/team/tasks/actions.ts, app/team/tasks/[taskId]/page.tsx</files>
  <read_first>
    - app/admin/clients/[clientId]/projects/[projectId]/actions.ts
    - app/admin/clients/[clientId]/projects/[projectId]/tasks/[taskId]/page.tsx
    - supabase/migrations/002_rls_policies.sql
    - .planning/phases/03-team-workflow-task-dashboard-and-editing/03-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: a team member can fetch their own assigned task by ID and see project/client context.
    - Test 2: a team member can save caption-only changes without changing file or status.
    - Test 3: a team member can change status independently from caption/file edits.
    - Test 4: an unassigned task ID resolves to notFound or an authorization-safe failure.
  </behavior>
  <action>
    Create `app/team/tasks/actions.ts` with team-only helpers and exported actions for caption/status/file updates. The file must:
    - call `createClient()`
    - verify `auth.getUser()` exists
    - confirm the current user is a `team_member`
    - load the target task through `task_assignments` / `tasks` joins so only the assigned member can mutate it
    - export concrete actions for `updateTeamTaskContentAction(taskId, { caption, status })` and `updateTeamTaskFilePathAction(taskId, filePath)`
    - revalidate `/team` and `/team/tasks/[taskId]`

    Create `app/team/tasks/[taskId]/page.tsx` as a dedicated server page per D-02. Fetch the assigned task plus joined project/client records and pass normalized data into a colocated client form. If the task is not visible to the signed-in team member, call `notFound()` instead of leaking authorization details.
  </action>
  <acceptance_criteria>
    - `app/team/tasks/actions.ts` exists and exports team-specific task update actions
    - the actions file validates both authenticated user presence and team ownership of the task
    - `app/team/tasks/[taskId]/page.tsx` exists and calls `notFound()` when the task is missing or inaccessible
    - the page fetch includes task, project, and client context
    - revalidation includes `/team` and `/team/tasks/[taskId]`
  </acceptance_criteria>
  <verify>
    <automated>npx eslint app/team/tasks/actions.ts app/team/tasks/[taskId]/page.tsx && npm run build</automated>
  </verify>
  <done>Team task data is loaded through a dedicated route and team-only server actions exist for safe mutations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the team task detail form with read-only context and independent editors</name>
  <files>app/team/tasks/[taskId]/task-detail-form.tsx</files>
  <read_first>
    - app/team/tasks/[taskId]/page.tsx
    - app/admin/clients/[clientId]/projects/[projectId]/tasks/[taskId]/task-edit-form.tsx
    - components/admin/copy-button.tsx
    - components/admin/design-file-uploader.tsx
    - components/admin/design-file-downloader.tsx
    - lib/utils.ts
  </read_first>
  <behavior>
    - Test 1: title, briefing, posting date, due date, and deadline render as read-only labeled values per D-03.
    - Test 2: caption edits can be saved without changing file/status.
    - Test 3: status changes can be saved without changing caption/file.
    - Test 4: caption area includes Copy button and existing design file includes Download button.
  </behavior>
  <action>
    Create `app/team/tasks/[taskId]/task-detail-form.tsx` as the interactive client boundary for TEAM-03/04/05/08. Implement D-03 and D-04 exactly:
    - read-only labeled blocks for `title`, `briefing` (rendered with `linkify()`), `posting date`, `due date`, and `deadline`
    - editable `caption` textarea with `CopyButton`
    - editable `status` select with `todo`, `in_progress`, `done`
    - design-file area that shows existing file name + `DesignFileDownloader`, plus `DesignFileUploader` for replace/upload
    - independent submit flows so caption/status/file can be saved without bundling every edit into one forced workflow

    Keep the team form simpler than the admin editor: no assignment controls, no task delete button, no title/date editing, no admin-only metadata. Use border-only black/white styling and keep the page functional at 375px width.
  </action>
  <acceptance_criteria>
    - `app/team/tasks/[taskId]/task-detail-form.tsx` exists as a `'use client'` component
    - the form renders read-only labeled values for title, briefing, posting date, due date, and deadline
    - the briefing block uses `linkify()`
    - the caption field renders `CopyButton`
    - the design-file section renders `DesignFileDownloader` when a file exists and `DesignFileUploader` for upload/replace
    - the status field uses the existing select component with `todo`, `in_progress`, and `done`
    - the file does not include assignment, delete, or title-edit controls
  </acceptance_criteria>
  <verify>
    <automated>npx eslint app/team/tasks/[taskId]/task-detail-form.tsx && npm run build</automated>
  </verify>
  <done>Team members can open a dedicated task page, review task context, and independently update caption, file, and status using team-safe controls.</done>
</task>

</tasks>

<verification>
- Task detail route is dedicated to `/team/tasks/[taskId]`.
- Read-only and editable fields are split exactly as the locked decisions specify.
- Copy/download affordances reuse existing proven components.
</verification>

<success_criteria>
- TEAM-03, TEAM-04, TEAM-05, and TEAM-08 are covered.
- D-02, D-03, and D-04 are fully implemented with no admin-only controls leaking into team UI.
</success_criteria>

<output>
After completion, create `.planning/phases/03-team-workflow-task-dashboard-and-editing/03-02-SUMMARY.md`
</output>
