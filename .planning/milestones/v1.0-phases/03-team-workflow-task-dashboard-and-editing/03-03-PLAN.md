---
phase: 03-team-workflow-task-dashboard-and-editing
plan: "03"
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - supabase/migrations/006_team_notification_insert_policy.sql
  - app/team/tasks/actions.ts
  - app/team/tasks/[taskId]/task-detail-form.tsx
autonomous: true
requirements:
  - TEAM-06
  - NOTIF-01
  - NOTIF-02
must_haves:
  truths:
    - "Team members can explicitly confirm Notify Assigner instead of status change auto-notifying (per D-05/D-08)"
    - "Confirming Notify Assigner marks the task done and creates an admin notification"
    - "The admin notification message follows the exact required string format"
  artifacts:
    - path: "supabase/migrations/006_team_notification_insert_policy.sql"
      provides: "RLS policy allowing team notification inserts"
      contains: "team_insert_notifications"
    - path: "app/team/tasks/actions.ts"
      provides: "notify assigner mutation"
      contains: "marked"
    - path: "app/team/tasks/[taskId]/task-detail-form.tsx"
      provides: "confirmation UI for Notify Assigner"
      contains: "Notify Assigner"
  key_links:
    - from: "app/team/tasks/[taskId]/task-detail-form.tsx"
      to: "app/team/tasks/actions.ts"
      via: "confirmed notify action"
      pattern: "notify"
    - from: "app/team/tasks/actions.ts"
      to: "notifications table"
      via: "insert with team_member_id null"
      pattern: "from\\('notifications'\\).*team_member_id"
---

<objective>
Add the explicit Notify Assigner workflow and the RLS support it needs.

Purpose: Let team members formally hand work back to admins with a deliberate, auditable in-app notification.
Output: Notify mutation, confirmation dialog, and new notification INSERT policy.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-team-workflow-task-dashboard-and-editing/03-CONTEXT.md
@.planning/phases/03-team-workflow-task-dashboard-and-editing/03-RESEARCH.md
@app/team/tasks/actions.ts
@app/team/tasks/[taskId]/task-detail-form.tsx
@supabase/migrations/001_initial_schema.sql
@supabase/migrations/002_rls_policies.sql
@app/admin/notifications/actions.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the team notification INSERT policy and notify mutation</name>
  <files>supabase/migrations/006_team_notification_insert_policy.sql, app/team/tasks/actions.ts</files>
  <read_first>
    - supabase/migrations/001_initial_schema.sql
    - supabase/migrations/002_rls_policies.sql
    - app/team/tasks/actions.ts
    - .planning/phases/03-team-workflow-task-dashboard-and-editing/03-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: assigned team member can insert a notification row with `team_member_id = NULL`.
    - Test 2: notify action writes message `[Member name] marked [task title] as done.` exactly.
    - Test 3: notify action sets `tasks.status` to `done` in the same flow.
    - Test 4: unassigned or unauthenticated callers are rejected.
  </behavior>
  <action>
    Add `supabase/migrations/006_team_notification_insert_policy.sql` implementing D-07 with a policy named `team_insert_notifications` on `public.notifications`. The policy must allow only `team_member` role users to `INSERT` rows where `team_member_id IS NULL`.

    Extend `app/team/tasks/actions.ts` with a dedicated `notifyAssignerAction(taskId)` implementing D-05 and D-06 exactly. It must:
    - verify the caller owns the assigned task
    - fetch the team member name and task title
    - insert into `notifications` with `team_member_id: null`
    - set the task status to `'done'`
    - use the exact message format `"[Member name] marked [task title] as done."`
    - revalidate `/team`, `/team/tasks/[taskId]`, `/admin`, and `/admin/notifications`

    Do not fold this logic into the generic status action. Keep it separate so ordinary status changes do not create notifications.
  </action>
  <acceptance_criteria>
    - migration file `006_team_notification_insert_policy.sql` exists
    - the migration contains `CREATE POLICY team_insert_notifications`
    - the migration restricts inserts to rows where `team_member_id IS NULL`
    - `app/team/tasks/actions.ts` exports a dedicated notify action separate from generic status updates
    - the notify action inserts into `notifications` and updates the task status to `done`
    - the notification message string matches `marked [task title] as done.` exactly
  </acceptance_criteria>
  <verify>
    <automated>npx eslint app/team/tasks/actions.ts && rg -n "team_insert_notifications|team_member_id IS NULL|marked .* as done" supabase/migrations/006_team_notification_insert_policy.sql app/team/tasks/actions.ts && npm run build</automated>
  </verify>
  <done>Team members have a dedicated, policy-backed notify mutation that creates the required admin notification and marks the task done.</done>
</task>

<task type="auto">
  <name>Task 2: Add confirmed Notify Assigner UI to the team task page</name>
  <files>app/team/tasks/[taskId]/task-detail-form.tsx</files>
  <read_first>
    - app/team/tasks/[taskId]/task-detail-form.tsx
    - app/team/tasks/actions.ts
    - components/ui/dialog.tsx
    - .planning/phases/03-team-workflow-task-dashboard-and-editing/03-CONTEXT.md
  </read_first>
  <action>
    Update `app/team/tasks/[taskId]/task-detail-form.tsx` to add a separate `Notify Assigner` action per D-05. Implement D-08 by wrapping the action in a confirmation dialog using the existing dialog primitives. The dialog must clearly state that confirming will notify the assigner and mark the task as done.

    Keep this button visually separate from generic save controls. On success, refresh the route feedback state so the user can see the task is now done. Do not auto-open the dialog on ordinary status changes, and do not merge the button into the status-select save flow.
  </action>
  <acceptance_criteria>
    - `app/team/tasks/[taskId]/task-detail-form.tsx` contains visible `Notify Assigner` copy
    - the file uses dialog primitives for a confirmation step before executing the mutation
    - the confirmation text states both outcomes: notification creation and status change to done
    - the button triggers the dedicated notify action, not the generic save/status action
    - generic status edits remain available without forcing notification
  </acceptance_criteria>
  <verify>
    <automated>npx eslint app/team/tasks/[taskId]/task-detail-form.tsx && rg -n "Notify Assigner|Dialog|done" app/team/tasks/[taskId]/task-detail-form.tsx && npm run build</automated>
  </verify>
  <done>The team task page exposes an explicit, confirmed Notify Assigner flow instead of silently notifying during ordinary task edits.</done>
</task>

</tasks>

<verification>
- Notify Assigner is a separate confirmed action.
- The action creates admin-facing notifications using the exact required string format.
- A new RLS policy enables the insert path without widening broader notification access.
</verification>

<success_criteria>
- TEAM-06, NOTIF-01, and NOTIF-02 are covered.
- D-05 through D-08 are implemented exactly.
- No realtime behavior is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/03-team-workflow-task-dashboard-and-editing/03-03-SUMMARY.md`
</output>
