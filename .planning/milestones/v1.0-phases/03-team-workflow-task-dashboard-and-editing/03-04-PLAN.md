---
phase: 03-team-workflow-task-dashboard-and-editing
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/admin/layout.tsx
  - app/admin/notifications/actions.ts
autonomous: true
requirements:
  - NOTIF-03
  - NOTIF-04
  - NOTIF-05
must_haves:
  truths:
    - "Admins can see an unread-count badge on a bell icon in the header/navigation"
    - "Existing admin notification page and dashboard continue to refresh correctly after read-state changes"
    - "Phase 03 continues to use navigation-time revalidation instead of realtime (per D-09)"
  artifacts:
    - path: "app/admin/layout.tsx"
      provides: "Admin bell icon with unread badge"
      contains: "Bell"
    - path: "app/admin/notifications/actions.ts"
      provides: "Notification read actions that refresh badge-bearing surfaces"
      contains: "revalidatePath"
  key_links:
    - from: "app/admin/layout.tsx"
      to: "/admin/notifications"
      via: "bell icon link"
      pattern: "notifications"
    - from: "app/admin/notifications/actions.ts"
      to: "app/admin/layout.tsx"
      via: "revalidation for unread badge"
      pattern: "revalidatePath"
---

<objective>
Wire the admin unread-notification badge into the shared layout.

Purpose: Make new team notifications visible everywhere admins navigate, while reusing the notification page and dashboard already built in Phase 2.
Output: Header bell icon with unread badge and layout-aware notification revalidation.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-team-workflow-task-dashboard-and-editing/03-CONTEXT.md
@.planning/phases/02-admin-core-client-project-and-task-management/02-05-SUMMARY.md
@app/admin/layout.tsx
@app/admin/page.tsx
@app/admin/notifications/page.tsx
@app/admin/notifications/actions.ts
@components/ui/badge.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add an unread bell badge to the admin layout</name>
  <files>app/admin/layout.tsx</files>
  <read_first>
    - app/admin/layout.tsx
    - app/admin/page.tsx
    - app/admin/notifications/page.tsx
    - components/ui/badge.tsx
    - .planning/phases/03-team-workflow-task-dashboard-and-editing/03-CONTEXT.md
  </read_first>
  <action>
    Update `app/admin/layout.tsx` to query the unread notification count server-side and render a bell icon link to `/admin/notifications` in the shared admin chrome. This implements NOTIF-04 and D-09 together: the count should refresh on normal page revalidation/navigation, not via Supabase Realtime.

    Render the bell in both desktop and mobile admin chrome so the unread badge is consistently visible. Use the existing black/white border-only styling and `Badge` component for the count. Do not add websocket/realtime code, polling intervals, or client-side subscriptions.
  </action>
  <acceptance_criteria>
    - `app/admin/layout.tsx` imports and renders a bell icon linked to `/admin/notifications`
    - unread notification count is queried server-side in the layout
    - the unread count is rendered with the existing badge component when count > 0
    - the bell is visible in both desktop and mobile admin chrome
    - the file contains no realtime subscription or polling logic
  </acceptance_criteria>
  <verify>
    <automated>npx eslint app/admin/layout.tsx && rg -n "Bell|/admin/notifications|Badge" app/admin/layout.tsx && npm run build</automated>
  </verify>
  <done>Admins see a shared bell icon with unread-count badge anywhere inside the admin workspace.</done>
</task>

<task type="auto">
  <name>Task 2: Extend notification actions to refresh badge-bearing layout surfaces</name>
  <files>app/admin/notifications/actions.ts</files>
  <read_first>
    - app/admin/notifications/actions.ts
    - app/admin/layout.tsx
    - app/admin/page.tsx
    - app/admin/notifications/page.tsx
  </read_first>
  <action>
    Update `app/admin/notifications/actions.ts` so `markNotificationAsRead` and `markAllNotificationsAsRead` revalidate every surface that can show notification state after Phase 03: `/admin`, `/admin/notifications`, and the admin layout tree that owns the bell badge. Keep the existing auth guard and server-action structure.

    Do not rewrite the notifications page or dashboard list; those already satisfy NOTIF-03 and NOTIF-05. This task ensures the newly introduced shared unread badge stays in sync with the existing admin notification UX.
  </action>
  <acceptance_criteria>
    - `app/admin/notifications/actions.ts` still contains the auth guard
    - both notification actions revalidate `/admin` and `/admin/notifications`
    - both notification actions also revalidate the layout-bearing admin surface used by the unread badge
    - no realtime subscription code is introduced
  </acceptance_criteria>
  <verify>
    <automated>npx eslint app/admin/notifications/actions.ts && rg -n "revalidatePath" app/admin/notifications/actions.ts && npm run build</automated>
  </verify>
  <done>Notification read-state changes update the dashboard list, notifications page, and unread badge without adding realtime complexity.</done>
</task>

</tasks>

<verification>
- The admin bell badge is part of shared layout chrome.
- Existing admin notifications page and dashboard remain the notification-detail surfaces.
- Notification state refreshes through standard revalidation only.
</verification>

<success_criteria>
- NOTIF-03, NOTIF-04, and NOTIF-05 are covered.
- D-09 is honored: navigation/revalidation refresh only, no realtime.
</success_criteria>

<output>
After completion, create `.planning/phases/03-team-workflow-task-dashboard-and-editing/03-04-SUMMARY.md`
</output>
