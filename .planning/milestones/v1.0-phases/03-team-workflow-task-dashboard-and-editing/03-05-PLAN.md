---
phase: 03-team-workflow-task-dashboard-and-editing
plan: "05"
type: execute
wave: 4
depends_on:
  - "01"
  - "02"
  - "03"
files_modified:
  - package.json
  - scripts/verify-team-rls.ts
autonomous: true
requirements:
  - TEAM-07
must_haves:
  truths:
    - "The project has an automated way to prove team members can only see and edit their own assigned tasks"
    - "The verification covers both read and write isolation"
  artifacts:
    - path: "scripts/verify-team-rls.ts"
      provides: "RLS regression script for team task isolation"
      contains: "verifyTeamRls"
    - path: "package.json"
      provides: "Runnable script entry for RLS verification"
      contains: "verify:team-rls"
  key_links:
    - from: "scripts/verify-team-rls.ts"
      to: "tasks/task_assignments/notifications"
      via: "authenticated Supabase verification flow"
      pattern: "from\\('tasks'\\)|from\\('task_assignments'\\)"
---

<objective>
Add an automated regression check for team-member data isolation.

Purpose: TEAM-07 is too important to leave as an assumption; this plan makes isolation verifiable after execution.
Output: Scripted verification for assigned-task-only reads and writes.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-team-workflow-task-dashboard-and-editing/03-CONTEXT.md
@.planning/phases/03-team-workflow-task-dashboard-and-editing/03-RESEARCH.md
@supabase/migrations/002_rls_policies.sql
@supabase/migrations/006_team_notification_insert_policy.sql
@app/team/tasks/actions.ts
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create a runnable team-RLS verification script</name>
  <files>scripts/verify-team-rls.ts, package.json</files>
  <read_first>
    - supabase/migrations/002_rls_policies.sql
    - supabase/migrations/006_team_notification_insert_policy.sql
    - package.json
    - app/team/tasks/actions.ts
  </read_first>
  <behavior>
    - Test 1: assigned team user can read their own task.
    - Test 2: assigned team user cannot read another member's task.
    - Test 3: assigned team user can update their own task caption or status.
    - Test 4: assigned team user cannot update another member's task.
  </behavior>
  <action>
    Create `scripts/verify-team-rls.ts` and add `"verify:team-rls": "node scripts/verify-team-rls.ts"` to `package.json`.

    The script should use `@supabase/supabase-js` with environment variables already present in the project, authenticate as a seeded/known team member, and run explicit assertions for TEAM-07:
    - query an assigned task and require success
    - query a non-assigned task and require zero rows or an authorization-safe failure
    - update the assigned task caption/status and require success
    - attempt the same update on a non-assigned task and require failure/no rows updated

    Exit with non-zero status on any failed assertion and print a short pass/fail summary to stdout. Keep the script lightweight and CLI-friendly; do not add a full test runner.
  </action>
  <acceptance_criteria>
    - `scripts/verify-team-rls.ts` exists
    - `package.json` contains a `verify:team-rls` script entry
    - the script checks both read isolation and write isolation
    - the script exits non-zero on failure
    - the script uses `@supabase/supabase-js` rather than introducing another dependency
  </acceptance_criteria>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.scripts['verify:team-rls']) process.exit(1)" && npx eslint scripts/verify-team-rls.ts</automated>
  </verify>
  <done>The repo contains a concrete automated check for team-member read/write isolation.</done>
</task>

<task type="auto">
  <name>Task 2: Run the isolation script against the current environment and document assumptions in the summary</name>
  <files>scripts/verify-team-rls.ts</files>
  <read_first>
    - scripts/verify-team-rls.ts
    - .planning/phases/03-team-workflow-task-dashboard-and-editing/03-VALIDATION.md
  </read_first>
  <action>
    Execute `node scripts/verify-team-rls.ts` after Plans 03-01 through 03-03 land. If the script depends on known seeded task IDs or test accounts, keep those assumptions inside the script constants or env-variable reads so future executions are repeatable. Record the exact data prerequisites and observed outcome in the plan summary.
  </action>
  <acceptance_criteria>
    - `node scripts/verify-team-rls.ts` is runnable as documented
    - the script prints a clear pass/fail result
    - the plan summary records any environment assumptions required for the check
  </acceptance_criteria>
  <verify>
    <automated>npm run build && node scripts/verify-team-rls.ts</automated>
  </verify>
  <done>TEAM-07 is backed by a repeatable automated isolation check instead of relying only on code inspection.</done>
</task>

</tasks>

<verification>
- Team-task isolation is tested through a runnable script, not just assumed from policy text.
- The check covers both reads and writes.
</verification>

<success_criteria>
- TEAM-07 is covered with automated verification.
- Phase 03 has an explicit regression guard for the most security-sensitive team behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/03-team-workflow-task-dashboard-and-editing/03-05-SUMMARY.md`
</output>
