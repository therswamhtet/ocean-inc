---
phase: 04-client-portal-public-read-only-views
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - components/portal/kanban-view.tsx
  - components/portal/kanban-task-card.tsx
  - __tests__/portal-kanban.test.tsx
autonomous: true
requirements: [CLIENT-03, CLIENT-06]
must_haves:
  truths:
    - "Client sees Kanban as a read-only board with expected status grouping"
    - "Every task card shows title, posting date, and pulsing status indicator"
  artifacts:
    - path: "components/portal/kanban-view.tsx"
      provides: "Read-only Kanban columns for portal"
      exports: ["PortalKanbanView"]
    - path: "components/portal/kanban-task-card.tsx"
      provides: "Task card rendering with status dot + date"
      exports: ["PortalKanbanTaskCard"]
  key_links:
    - from: "components/portal/kanban-view.tsx"
      to: "components/portal/kanban-task-card.tsx"
      via: "task mapping"
      pattern: "map\(.*PortalKanbanTaskCard"
---

<objective>
Implement the portal Kanban view with locked 3-column structure and overdue visual status behavior.

Purpose: Deliver the primary client-facing status view first.
Output: Read-only Kanban board + task cards + focused component test coverage.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-client-portal-public-read-only-views/04-CONTEXT.md
@.planning/phases/04-client-portal-public-read-only-views/04-01-PLAN.md

<interfaces>
From lib/portal/types.ts (from 04-01):
```ts
export type PortalTaskStatus = 'todo' | 'in_progress' | 'done'
export type PortalTask = {
  id: string
  title: string
  caption: string | null
  design_file_path: string | null
  posting_date: string | null
  status: PortalTaskStatus
}
```

From components/ui/status-dot.tsx:
```ts
export function StatusDot({ status }: { status: 'todo' | 'in_progress' | 'done' | 'overdue' })
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build read-only portal Kanban board and card components</name>
  <files>components/portal/kanban-view.tsx, components/portal/kanban-task-card.tsx, __tests__/portal-kanban.test.tsx</files>
  <read_first>components/admin/kanban-board.tsx, components/admin/kanban-card.tsx, components/ui/status-dot.tsx, .planning/phases/04-client-portal-public-read-only-views/04-CONTEXT.md</read_first>
  <behavior>
    - Board renders three columns (`todo`, `in_progress`, `done`) per D-01.
    - Overdue tasks stay in their status column and show overdue visual dot (D-01).
    - Card shows title + posting date + animated status dot (D-02).
  </behavior>
  <acceptance_criteria>
    - `components/portal/kanban-view.tsx` exports `PortalKanbanView` and does not import dnd-kit.
    - File contains three explicit column labels: `To Do`, `In Progress`, `Done` (per D-01).
    - `components/portal/kanban-task-card.tsx` renders `StatusDot` and posting date text.
    - `__tests__/portal-kanban.test.tsx` verifies 3-column rendering and overdue-dot behavior.
    - Action note present for traceability: 3 columns used per D-01 (despite roadmap wording of 4).
  </acceptance_criteria>
  <action>Create read-only portal Kanban components using D-01/D-02. Implement exactly 3 columns (`todo`, `in_progress`, `done`) and compute overdue as visual status only (red pulsing dot) while preserving the task's logical column; explicitly note this is per locked D-01 even though roadmap text says four columns. Do not include drag/drop, status mutation, or server actions. Add component tests for column count, card content, and overdue visual mapping.</action>
  <verify>
    <automated>npm run test -- __tests__/portal-kanban.test.tsx</automated>
  </verify>
  <done>Kanban is fully read-only and matches locked card/column behavior for client portal.</done>
</task>

<task type="auto">
  <name>Task 2: Keep Kanban view contract-compatible with portal shell</name>
  <files>components/portal/kanban-view.tsx</files>
  <read_first>components/portal/portal-shell.tsx, lib/portal/types.ts, .planning/phases/04-client-portal-public-read-only-views/04-01-PLAN.md</read_first>
  <acceptance_criteria>
    - `PortalKanbanView` accepts props typed from `lib/portal/types.ts` (no local duplicate task type).
    - Component remains pure render logic (no Supabase calls, no route navigation side effects).
    - Empty-column and empty-board states are rendered without runtime errors.
  </acceptance_criteria>
  <action>Align the Kanban component signature with the shared portal contracts from 04-01 so shell integration is plug-and-play for later plans. Keep implementation deterministic and read-only, including empty-state placeholders for missing tasks in any column.</action>
  <verify>
    <automated>npm run lint -- components/portal/kanban-view.tsx components/portal/kanban-task-card.tsx</automated>
  </verify>
  <done>Kanban component is contract-safe and ready for shell + modal wiring in later wave.</done>
</task>

</tasks>

<verification>
Run `npm run test -- __tests__/portal-kanban.test.tsx` and confirm no mutation dependencies exist.
</verification>

<success_criteria>
- Portal Kanban renders 3 locked columns per D-01.
- Task cards include required display fields per D-02 / CLIENT-06.
- Overdue is visual-only and does not create a fourth column.
</success_criteria>

<output>
After completion, create `.planning/phases/04-client-portal-public-read-only-views/04-02-SUMMARY.md`
</output>
