# Deferred Items

## 2026-04-04

- Build warning: Next.js reports `middleware` convention deprecation (`use "proxy" instead`).
  - Scope: pre-existing project-wide infrastructure outside Plan 04-01 task files.
  - Action: deferred to a dedicated infrastructure cleanup plan.
