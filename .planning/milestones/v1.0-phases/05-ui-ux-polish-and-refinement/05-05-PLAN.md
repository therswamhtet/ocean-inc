# Plan 05-05: Bug Fixes + Regression Sweep

**Wave:** 3 (final polish)
**Phase:** 05-ui-ux-polish-and-refinement
**Date:** 2026-04-05
**Estimated Duration:** 6 min

## Goal

Fix remaining lint warnings, clean up unused imports, and do a full regression sweep across all surfaces to confirm nothing broke during this phase's changes.

## Tasks

### 05-05-01: Fix lint warnings from Phase 05

**Audit** all files touched this phase for:
- Unused imports (Sheet-related imports in admin/team layouts that aren't consumed)
- React Hook Form compiler warnings in task-create-form
- Any TypeScript errors or warnings

**Fix** all remaining lint issues. Target: zero new warnings introduced by Phase 05.

Note: Pre-existing Phase 4 test file lint errors can remain as they are outside Phase 05 scope.

### 05-05-02: Full build verification

**Run** `npm run build` and verify:
- Zero compilation errors
- No new ESLint warnings from Phase 05 changes
- All routes render without crashing
- Server actions compile correctly

### 05-05-03: Terminology audit — "admin console" → "management panel"

**Grep** the entire codebase for:
```
rg -i "admin console" --type tsx
rg -i "adminconsole" --type ts
```

**Replace** any remaining occurrences with "management panel" or appropriate localized term from `lib/labels.ts`.

## Verification

- [ ] Zero lint warnings from Phase 05 files (pre-existing test warnings excluded)
- [ ] `npm run build` completes with zero errors
- [ ] No remaining "admin console" text anywhere in the codebase
- [ ] All Phase 05 changes work together without conflicts

## Dependencies

- All prior Phase 05 plans (05-01 through 05-04)

## Notes

- Only fix lint issues introduced or touched by Phase 05
- Pre-existing Phase 4 test file warnings are acknowledged but out of scope

---

*Plan: 05-05 | Wave: 3 | Phase: 05-ui-ux-polish-and-refinement*
