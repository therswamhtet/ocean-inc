---
phase: 06-comprehensive-ui-ux-refinement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/admin/sidebar.tsx
  - app/admin/layout.tsx
autonomous: true
requirements:
  - UI-11
  - UI-12
  - UI-13
  - UI-14
  - UI-30
  - UI-31
  - UI-32
user_setup: []

must_haves:
  truths:
    - "Sidebar has a 'Tasks' nav item alongside Dashboard, Clients, Team Members"
    - "Mobile sidebar is on the left side, not right"
    - "Notification icon removed from header"
    - "Header says 'Orca Digital' (bold) without 'Management Panel'"
  artifacts:
    - path: "app/admin/sidebar.tsx"
      provides: "Sidebar nav items including new Tasks link"
      exports: ["AdminSidebar"]
    - path: "app/admin/layout.tsx"
      provides: "Admin layout with updated header branding"
      exports: ["default"]
    - path: "app/admin/tasks/page.tsx"
      provides: "New Tasks page shell with categorized task lists"
      exports: ["default"]
  key_links:
    - from: "app/admin/sidebar.tsx"
      to: "app/admin/tasks/page.tsx"
      via: "Link href=/admin/tasks"
      pattern: "href.*admin/tasks"
    - from: "app/admin/layout.tsx"
      to: "app/admin/sidebar.tsx"
      via: "import/usage"
      pattern: "AdminSidebar"
---

<objective>
Update admin header branding and add Tasks tab to sidebar. Changes the header from "Orca Digital Admin" / "Management Panel" to "Orca Digital" with Poppins bold, adds Tasks navigation to sidebar, repositions mobile sidebar to left, and removes notification icon from header.

Purpose: Establish the updated brand identity and new Tasks navigation entry point.
Output: Updated header, updated sidebar with Tasks link, new Tasks page shell, mobile sidebar repositioned.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-comprehensive-ui-ux-refinement/06-CONTEXT.md

<interfaces>
From app/admin/sidebar.tsx:
```typescript
export function AdminSidebar({ mobile = false }: { mobile?: boolean })
// navItems array drives navigation links
const navItems = [
  { href: "/admin", label: "Dashboard", icon: LayoutDashboard },
  { href: "/admin/clients", label: "Clients", icon: FolderKanban },
  { href: "/admin/team", label: "Team Members", icon: UserCircle2 },
]
```

From app/admin/layout.tsx:
```typescript
// Renders header with "Orca Digital" branding text
// Renders mobile sidebar toggle (hamburger)
// Renders AdminSidebar inside mobile sheet
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update header branding and remove notification icon</name>
  <files>app/admin/layout.tsx</files>
  <read_first>
    - app/admin/layout.tsx
  </read_first>
  <action>
    In app/admin/layout.tsx:
    1. Change header text from "Orca Digital Admin" to "Orca Digital"
    2. Apply font-bold className to "Orca Digital" text (matching the Poppins font already imported)
    3. Remove "Management Panel" text entirely if present in header
    4. Remove notification bell icon from header area (UI-14) — it remains in the layout, just removes it from the header text zone

    Target: Header contains only the Orca Digital logo/brand text (bold), plus mobile hamburger toggle on the right.
  </action>
  <acceptance_criteria>
    - app/admin/layout.tsx contains text "Orca Digital" (without "Admin")
    - "Orca Digital" has font-bold or equivalent className
    - "Management Panel" text is NOT present in layout.tsx
    - No notification bell icon component in the header area of layout.tsx
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Orca Digital" app/admin/layout.tsx && ! grep -q "Management Panel" app/admin/layout.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Header displays "Orca Digital" in bold Poppins, no "Management Panel" text, no notification icon in header</done>
</task>

<task type="auto">
  <name>Task 2: Add Tasks tab to sidebar and reposition mobile sidebar</name>
  <files>app/admin/sidebar.tsx</files>
  <read_first>
    - app/admin/sidebar.tsx
    - packages.json
  </read_first>
  <action>
    In app/admin/sidebar.tsx:
    1. Import a Tasks icon from lucide-react (use `ListTodo` from lucide-react)
    2. Add new nav item: `{ href: "/admin/tasks", label: "Tasks", icon: ListTodo }` to navItems array — position after Dashboard, before Clients
    3. Mobile sidebar: the `mobile` prop currently adds `pt-4` padding — change mobile positioning so sidebar appears on the LEFT side instead of right. In the mobile sheet trigger and sheet component in layout.tsx, change sheet position from right side (`side="right"`) to left side (`side="left"`).

    In app/admin/layout.tsx:
    4. Find the Sheet component wrapping mobile sidebar and change its `side` prop from "right" to "left" (or equivalent positioning)
  </action>
  <acceptance_criteria>
    - app/admin/sidebar.tsx contains "ListTodo" import from lucide-react (or similar tasks icon)
    - app/admin/sidebar.tsx navItems array contains object with href "/admin/tasks" and label "Tasks"
    - app/admin/layout.tsx mobile Sheet has side="left" or equivalent left-positioning
  </acceptance_criteria>
  <verify>
    <automated>grep -q '"/admin/tasks"' app/admin/sidebar.tsx && grep -q 'side.*=.*"left"' app/admin/layout.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Sidebar has Tasks tab with icon, mobile sidebar opens from left instead of right</done>
</task>

<task type="auto">
  <name>Task 3: Create Tasks page shell with categorized lists</name>
  <files>app/admin/tasks/page.tsx</files>
  <read_first>
    - app/admin/page.tsx
    - app/admin/clients/page.tsx
    - lib/supabase/server.ts
  </read_first>
  <action>
    Create app/admin/tasks/page.tsx — a Server Component that:
    1. Uses getUser() to verify admin auth
    2. Queries tasks from Supabase with date categorization:
       - "Today's Tasks": tasks where DATE(posting_date) = TODAY or DATE(due_date) = TODAY
       - "Upcoming": tasks where posting_date/due_date > TODAY and status != 'done'
       - "Due": tasks where due_date < TODAY and status != 'done'
    3. Renders three sections with task counts and compact task list items showing: project name (via join), title, status dot
    4. Uses the same table/card layout patterns from existing admin pages

    This is a page shell — no editing capability, just viewing categorized tasks sorted by relevance.
  </action>
  <acceptance_criteria>
    - File app/admin/tasks/page.tsx exists
    - Contains "use server" or server-side Supabase query
    - Contains getUser() call for auth check
    - Contains three sections: "Today", "Upcoming", and "Due" (or similar category labels)
    - Queries tasks table with date filtering
  </acceptance_criteria>
  <verify>
    <automated>test -f app/admin/tasks/page.tsx && grep -q "getUser" app/admin/tasks/page.tsx && grep -q "Today" app/admin/tasks/page.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Tasks page exists at /admin/tasks with three categorized sections (Today, Upcoming, Due), accessible from sidebar</done>
</task>

</tasks>

<verification>
- Sidebar renders all 4 nav items (Dashboard, Tasks, Clients, Team Members) with icons
- /admin/tasks page loads and shows categorized task sections
- Header displays "Orca Digital" (bold) without "Management Panel"
- Mobile sidebar opens from left side
</verification>

<success_criteria>
- [ ] Header branding updated to "Orca Digital" in bold
- [ ] "Management Panel" text removed
- [ ] Notification icon removed from header
- [ ] Tasks nav item in sidebar with ListTodo icon
- [ ] Tasks page exists at /admin/tasks with 3 sections
- [ ] Mobile sidebar opens from left
- [ ] No TypeScript errors (next build passes)
</success_criteria>

<output>
After completion, create `.planning/phases/06-comprehensive-ui-ux-refinement/06-01-SUMMARY.md`
</output>
