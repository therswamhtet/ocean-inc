---
phase: 06-comprehensive-ui-ux-refinement
plan: 05
type: execute
wave: 2
depends_on:
  - "06-01"
  - "06-02"
  - "06-03"
  - "06-04"
files_modified:
  - app/admin/sidebar.tsx
  - app/admin/layout.tsx
  - app/admin/clients/[clientId]/projects/[projectId]/task-view-toggle.tsx
  - app/admin/team/page.tsx
  - lib/labels.ts
autonomous: true
requirements:
  - UI-20
  - UI-21
  - UI-22
  - UI-27
user_setup: []

must_haves:
  truths:
    - "Navigation items show icons instead of text-heavy labels"
    - "Project sections display icons for context (name, month, status)"
    - "Task detail sidebar has no empty space below assigned team members"
    - "Icons are consistent throughout the admin interface"
  artifacts:
    - path: "app/admin/sidebar.tsx"
      provides: "Sidebar nav with icon+label pattern"
      exports: ["AdminSidebar"]
    - path: "app/admin/layout.tsx"
      provides: "Header with updated branding"
      exports: ["default"]
    - path: "app/admin/clients/[clientId]/projects/[projectId]/task-view-toggle.tsx"
      provides: "View toggle with icon-labeled tabs"
      exports: ["TaskViewToggle"]
  key_links:
    - from: "app/admin/sidebar.tsx"
      to: "lucide-react icons"
      via: "imported icon components"
      pattern: "from 'lucide-react'"
---

<objective>
Add consistent icons throughout the admin interface, fix task detail sidebar layout, and ensure visual consistency across all surfaces.

Purpose: Reduce text-heavy navigation by adding icons, tighten task detail layout to remove empty space gaps.
Output: Updated sidebar with icons, icon-labeled view toggle, tightened task detail, consistent icon usage.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-comprehensive-ui-ux-refinement/06-CONTEXT.md
@app/admin/sidebar.tsx

<interfaces>
From app/admin/sidebar.tsx:
```typescript
const navItems = [
  { href: "/admin", label: "Dashboard", icon: LayoutDashboard },
  { href: "/admin/clients", label: "Clients", icon: FolderKanban },
  { href: "/admin/team", label: "Team Members", icon: UserCircle2 },
]
export function AdminSidebar({ mobile = false }: { mobile?: boolean })
```

From app/admin/clients/[clientId]/projects/[projectId]/task-view-toggle.tsx:
```typescript
export function TaskViewToggle({ initialTasks, projectId, clientId })
// Renders toggle buttons: "List" and "Kanban" as text buttons
// No icons on toggle buttons currently
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add icons to view toggles, team page, and task detail sidebar</name>
  <files>
    - app/admin/clients/[clientId]/projects/[projectId]/task-view-toggle.tsx
    - app/admin/team/page.tsx
  </files>
  <read_first>
    - app/admin/clients/[clientId]/projects/[projectId]/task-view-toggle.tsx
    - app/admin/team/page.tsx
  </read_first>
  <action>
    In app/admin/clients/[clientId]/projects/[projectId]/task-view-toggle.tsx:
    1. Import List and LayoutGrid icons from lucide-react:
       ```typescript
       import { List, LayoutGrid } from 'lucide-react'
       ```
    2. Replace text-only toggle buttons with icon + text:
       ```tsx
       {nextView === 'list' ? (
         <>
           <List className="h-4 w-4" />
           List
         </>
       ) : (
         <>
           <LayoutGrid className="h-4 w-4" />
           Kanban
         </>
       )}
       ```

    In app/admin/team/page.tsx:
    3. Import icons for team page section headers:
       - Import `Mail`, `Calendar`, `UserPlus` from lucide-react
    4. Add icon to team page header: `UserPlus className="h-5 w-5"` beside "Team Members" heading
    5. Add icons to table column headers (optional, if text-heavy):
       - Name: User icon
       - Email: Mail icon
       - Joined: Calendar icon
  </action>
  <acceptance_criteria>
    - task-view-toggle.tsx imports List and LayoutGrid from lucide-react
    - Toggle buttons render icon alongside text (or instead of text on mobile)
    - team/page.tsx imports lucide-react icons
    - Team page header includes icon element
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'LayoutGrid' app/admin/clients/\[clientId\]/projects/\[projectId\]/task-view-toggle.tsx && grep -q 'lucide-react' app/admin/team/page.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>View toggles have icons, team page has icons in headers and sections</done>
</task>

<task type="auto">
  <name>Task 2: Fix task detail sidebar empty space and add final icon consistency</name>
  <files>
    - app/admin/layout.tsx
  </files>
  <read_first>
    - app/admin/layout.tsx
    - app/admin/sidebar.tsx
  </read_first>
  <action>
    In app/admin/layout.tsx:
    1. Update mobile header branding (line 75-76):
       - Change `Orca Digital` to use `font-bold` for Poppins bold: `className="font-bold"`
       - Keep `text-xs uppercase tracking-[0.18em]` for the "Orca Digital" label
       - Change `h1` text from "Admin" to remove it entirely (UI-32: "Management Panel" text already addressed in Plan 01 Task 1, but also remove standalone "Admin" from mobile header)
       - Mobile header should be: `Orca Digital` (bold, uppercase tracking) only
    
    2. Update desktop header branding (line 36-37):
       - Ensure `Orca Digital` text uses `font-bold` className
       - Ensure `{LABELS.common.adminPanel}` is REMOVED if not already (from Plan 01 Task 1)
    
    3. In sidebar.tsx, verify all nav items have proper icons (already from Plan 01, but double-check):
       - Dashboard → LayoutDashboard ✓
       - Tasks → ListTodo ✓
       - Clients → FolderKanban ✓
       - Team Members → Users (change from UserCircle2 to Users for consistency)

    4. Task detail sidebar empty space fix (UI-27):
       Find the task detail page component — search for it:
       ```bash
       grep -r "detail" app/admin/ --include="*.tsx" -l
       grep -r "task-detail\|TaskDetail" app/admin/ --include="*.tsx" -l
       grep -r "edit.*task\|task.*edit" app/admin/ --include="*.tsx" -l
       ```
       Look for the task detail/edit page (likely in app/admin/clients/[clientId]/projects/[projectId]/tasks/ or similar).
       In that file, fix the layout to remove empty space below the assigned team member section:
       - Ensure the form container uses `w-full` without a fixed max-width sidebar
       - Remove any `max-w-*` or `w-*` constraints on the form wrapper that cause whitespace
       - Ensure sidebar content fills the available height properly with `flex-1` or equivalent
  </action>
  <acceptance_criteria>
    - app/admin/layout.tsx header has Orca Digital with font-bold
    - app/admin/layout.tsx does NOT contain LABELS.common.adminPanel in the header
    - sidebar.tsx uses Users icon for Team Members (not UserCircle2)
    - Task detail file found and fixed to remove empty space
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'Orca Digital' app/admin/layout.tsx && grep -q 'font-bold' app/admin/layout.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Icon consistency throughout admin, task detail sidebar has no empty space, branding uses Poppins bold</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify comprehensive UI/UX changes</name>
  <what-built>
    All 26 UI/UX requirements from Phase 6:
    - Dashboard calendar fixes (mobile sizing, no upcoming, clickable tasks)
    - Sidebar Tasks tab, mobile repositioning, notification icon removed
    - Client color differentiation and logo upload
    - Timeline view removed, Kanban default
    - Icon system throughout
    - Create project as modal
    - Task creation simplified (no due date)
    - Task detail sidebar spacing fixed
    - Team invite consolidated
    - Branding updated (Orca Digital, Poppins bold, no Management Panel)
  </what-built>
  <how-to-verify>
    1. Start dev server: npm run dev
    2. Visit /admin and verify:
       a. Header shows "Orca Digital" (bold) without "Management Panel" or "Admin"
       b. Sidebar has Tasks tab with icon
       c. Dashboard calendar is readable on narrow viewport
       d. No "Upcoming" section on dashboard
    3. Click /admin/tasks — page loads with categorized task lists
    4. Visit /admin/clients — clients show color indicators
    5. Visit /admin/clients/[id] — Create Project opens in modal dialog
    6. Visit /admin/clients/[id]/projects/[id] — defaults to Kanban view
    7. Click Create Task — modal shows only posting date and deadline (no due date)
    8. Visit /admin/team — single "Generate" button with icon
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- All admin pages have consistent icon usage
- Header shows "Orca Digital" in bold, no "Management Panel", no "Admin" subtitle
- Sidebar has 4 nav items with icons (Dashboard, Tasks, Clients, Team Members)
- Task detail sidebar has no empty space gap
- Human verification checkpoint confirms all 26 requirements
- next build passes
</verification>

<success_criteria>
- [ ] View toggles display List and Kanban with icons
- [ ] Team page has icons in header and table columns
- [ ] Orca Digital branding uses font-bold
- [ ] No "Management Panel" or standalone "Admin" text in header
- [ ] Sidebar uses Users icon for Team Members
- [ ] Task detail sidebar empty space resolved
- [ ] Human verification checkpoint completed for all 26 UI/UX requirements
- [ ] next build passes
</success_criteria>

<output>
After completion, create `.planning/phases/06-comprehensive-ui-ux-refinement/06-05-SUMMARY.md`
</output>
