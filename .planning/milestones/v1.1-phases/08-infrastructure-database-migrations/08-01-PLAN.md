---
phase: 08-infrastructure-database-migrations
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/012_v11_columns.sql
  - lib/types/database.ts
autonomous: true
requirements:
  - CLIENT-11
  - CLIENT-12
  - USER-01
must_haves:
  truths:
    - "clients table gets description TEXT (nullable) and is_active BOOLEAN DEFAULT true columns"
    - "team_members table gets username TEXT UNIQUE (nullable) column"
    - "All three columns in one migration file: 012_v11_columns.sql"
  artifacts:
    - path: "supabase/migrations/012_v11_columns.sql"
      provides: "Single migration adding clients.description, clients.is_active, team_members.username"
      min_lines: 15
---

<objective>
Add new database columns required for all subsequent v1.1 features

Purpose: Foundation migration so Phases 9-11 can use client description, client blocking, and username system
Output: Migration 012 and updated TypeScript types
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/phases/08-infrastructure-database-migrations/08-CONTEXT.md
@supabase/migrations/001_initial_schema.sql
@supabase/migrations/008_client_color_and_logo.sql
@supabase/migrations/011_fix_rls_cascade_delete.sql
</execution_context>

<context>
## Key Decisions (from CONTEXT.md)
- D-01 through D-04: Client description — TEXT, nullable, optional, max ~200 chars
- D-05 through D-09: Client is_active — BOOLEAN DEFAULT true; blocked clients behave as if they don't exist in portal
- D-10 through D-15: Username — TEXT UNIQUE, nullable, user-chosen @handle, collected on invite registration
- D-16: Single migration 012_v11_columns.sql adding all three columns
- D-17: No RLS policy changes beyond is_active check needed in portal query (handled in select, not RLS)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration 012 adding clients.description, clients.is_active, team_members.username</name>
  <files>supabase/migrations/012_v11_columns.sql</files>
  <action>
    Create `supabase/migrations/012_v11_columns.sql` with three ALTER TABLE statements:

    1. `ALTER TABLE clients ADD COLUMN description TEXT;` — nullable, no default
    2. `ALTER TABLE clients ADD COLUMN is_active BOOLEAN NOT NULL DEFAULT true;` — defaults true for all existing clients
    3. `ALTER TABLE team_members ADD COLUMN username TEXT UNIQUE;` — nullable (no NOT NULL since NULL allows multiple rows with NULL), UNIQUE for non-null values

    Follow the pattern from `supabase/migrations/008_client_color_and_logo.sql` — simple ALTER TABLE statements, no triggers needed.

    Add a CHECK constraint on the teams_members.username length (3-20 characters) when not null:
    `ALTER TABLE team_members ADD CONSTRAINT username_length CHECK (username IS NULL OR (char_length(username) >= 3 AND char_length(username) <= 20));`

    Add constraint for allowed characters (lowercase alphanumeric and hyphens only):
    `ALTER TABLE team_members ADD CONSTRAINT username_format CHECK (username IS NULL OR username ~ '^[a-z0-9-]+$');`

    The CHECK constraint enforces the format at the database level regardless of application input.
  </action>
  <read_first>
    - supabase/migrations/001_initial_schema.sql (original table definitions)
    - supabase/migrations/008_client_color_and_logo.sql (pattern for ALTER TABLE)
  </read_first>
  <acceptance_criteria>
    - supabase/migrations/012_v11_columns.sql exists
    - File contains ALTER TABLE clients ADD COLUMN description TEXT
    - File contains ALTER TABLE clients ADD COLUMN is_active BOOLEAN NOT NULL DEFAULT true
    - File contains ALTER TABLE team_members ADD COLUMN username TEXT UNIQUE
    - Username has CHECK constraint for length (3-20 chars) and format (lowercase alphanumeric + hyphens)
    - All three columns use the exact column names from the decisions
  </acceptance_criteria>
  <verify>
    <automated>
      test -f supabase/migrations/012_v11_columns.sql && echo "MIGRATION_EXISTS" || echo "MISSING"
      grep -c "description" supabase/migrations/012_v11_columns.sql
      grep -c "is_active" supabase/migrations/012_v11_columns.sql
      grep -c "username" supabase/migrations/012_v11_columns.sql
    </automated>
  </verify>
  <done>Migration 012 created: clients.description (TEXT, nullable), clients.is_active (BOOLEAN DEFAULT true), team_members.username (TEXT UNIQUE, nullable) with validation constraints</done>
</task>

</tasks>

<verification>
- supabase/migrations/012_v11_columns.sql has three column additions
- Migration applies cleanly (can be checked with npx supabase db push --dry-run or supabase migration list)
- No RLS policies modified (D-17)
- is_active in portal query is a select filter, not RLS
</verification>

<success_criteria>
- Migration 012 adds all three columns from decisions D-05, D-01, D-10
- Existing clients get is_active = true by default
- Existing team_members get NULL username (prompted later, D-14)
- Username UNIQUE constraint enforces uniqueness
- Username CHECK constraints enforce format and length
</success_criteria>
