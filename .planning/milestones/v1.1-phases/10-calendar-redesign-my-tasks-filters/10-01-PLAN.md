---
phase: 10-calendar-redesign-my-tasks-filters
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - components/portal/calendar-view.tsx
autonomous: true
requirements:
  - CAL-01
  - CAL-02
  - CAL-03
  - CAL-04

must_haves:
  truths:
    - Calendar day cells render as square blocks
    - All tasks display fully without clipping
    - Calendar has no overflow at mobile (375px) or desktop widths
  artifacts:
    - path: components/portal/calendar-view.tsx
      provides: Square day cells with no overflow
  key_links:
    - from: MonthDayCell
      to: TaskPill
      via: task prop
      pattern: maxVisible overflow

---

<objective>
Redesign calendar to render with clean square day blocks and no overflow. Per D-01, D-02, D-03, D-04, D-05, D-06, D-07.

Purpose: Clean month grid with square cells that handle overflow gracefully
Output: Updated calendar-view.tsx with square blocks, expand-on-click overflow
</objective>

<context>
@components/portal/calendar-view.tsx
@lib/portal/calendar-utils.ts
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update MonthDayCell to square blocks</name>
  <files>components/portal/calendar-view.tsx</files>
  <action>
Per D-01: Remove rounded-lg border from MonthDayCell div. Change from:
  className={cn('relative cursor-pointer rounded-lg border p-2 transition...')}
To:
  className={cn('relative cursor-pointer border p-2 transition...')}

Also per D-02 and D-03: Keep maxVisible={3} pills by default, keep expand-on-click pattern with "+N more" indicator. Keep TaskPill truncation (text-[11px], truncate class) per D-07.
  </action>
  <verify>
grep -n "rounded-lg" components/portal/calendar-view.tsx | head -5
grep -n "maxVisible" components/portal/calendar-view.tsx
  </verify>
  <done>MonthDayCell renders as square blocks (no rounded-lg), tasks expand on click with overflow indicator</done>
</task>

<task type="auto">
  <name>Task 2: Verify mobile responsiveness</name>
  <files>components/portal/calendar-view.tsx</files>
  <action>
Per D-05: Keep overflow-x-auto wrapper with min-w-[560px] for desktop scroll.
Per D-06: Ensure cells shrink proportionally at 375px mobile - verify text sizes reduce and 44px touch targets maintained.
Check that TaskPill uses text-[11px] font and truncate class for title overflow.
  </action>
  <verify>
grep -n "min-w-\[560px\]\|overflow-x-auto\|text-\[11px\]\|min-h-\[44px\]" components/portal/calendar-view.tsx
  </verify>
  <done>Calendar grid uses overflow-x-auto with min-w-[560px], mobile cells shrink proportionally, 44px touch targets</done>
</task>

</tasks>

<verification>
Calendar renders without horizontal overflow at 375px and desktop widths
</verification>

<success_criteria>
Square day blocks (no rounded corners), tasks display fully without clipping, no overflow on mobile
</success_criteria>

<output>
After completion, create `.planning/phases/10-calendar-redesign-my-tasks-filters/10-01-SUMMARY.md`
</output>
