---
phase: 10-calendar-redesign-my-tasks-filters
plan: '02'
type: execute
wave: 1
depends_on: []
files_modified:
  - components/admin/dashboard-inner.tsx
autonomous: true
requirements:
  - TASKS-01
  - TASKS-02

must_haves:
  truths:
    - My Tasks view shows filter buttons for Today, This week, This month
    - Selecting a filter updates the displayed task list
  artifacts:
    - path: components/admin/dashboard-inner.tsx
      provides: Filter toggle buttons for time period selection
  key_links:
    - from: DashboardMyTasks
      to: filter state
      via: useState
      pattern: Today/This week/This month

---

<objective>
Add time period filter buttons to My Tasks view: Today, This week, This month. Per D-08, D-09, D-10, D-11, D-12, D-13, D-14.

Purpose: Team members can filter assigned tasks by time period
Output: Toggle button group with client-side filtering of tasks array
</objective>

<context>
@components/admin/dashboard-inner.tsx
@app/admin/page.tsx
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add filter state and toggle button group</name>
  <files>components/admin/dashboard-inner.tsx</files>
  <action>
Per D-08, D-09, D-10, D-11:
Add useState for filter selection ('today' | 'week' | 'month', default 'week').
Add toggle button group above task list section (lines 506-516):
- Three buttons: "Today", "This week", "This month"
- Style as segmented control: active = bg-[#222222] text-white, inactive = text-muted-foreground
- Use toggle pattern consistent with calendar mode toggle (mode === nextMode pattern)

Per D-12, D-13, D-14:
- "Today" = tasks where posting_date === current date (yyyy-MM-dd format)
- "This week" = tasks where posting_date falls within Sun-Sat of current week
- "This month" = tasks where posting_date falls within current calendar month
Filtering is client-side after tasks are fetched (per D-18).
  </action>
  <verify>
grep -n "useState" components/admin/dashboard-inner.tsx | head -3
grep -n "bg-\[#222222\]" components/admin/dashboard-inner.tsx
  </verify>
  <done>Filter state exists with default "This week", toggle buttons styled as segmented control</done>
</task>

<task type="auto">
  <name>Task 2: Implement filter logic</name>
  <files>components/admin/dashboard-inner.tsx</files>
  <action>
Add filter function that receives tasks array and returns filtered subset based on selected filter.
Use date-fns for date comparisons:
- startOfWeek, endOfWeek for "This week" (ISO week Sun-Sat)
- startOfMonth, endOfMonth for "This month"
- format(new Date(), 'yyyy-MM-dd') for "Today" comparison

Apply filter before splitting into overdue/inProgress/todo groups.
Tasks prop is already passed to DashboardMyTasks from app/admin/page.tsx (lines 79-95).
  </action>
  <verify>
grep -n "startOfWeek\|endOfWeek\|startOfMonth\|endOfMonth" components/admin/dashboard-inner.tsx
grep -n "filter" components/admin/dashboard-inner.tsx
  </verify>
  <done>Filter correctly narrows displayed tasks based on selected time period</done>
</task>

</tasks>

<verification>
My Tasks section displays filter buttons and selecting a filter updates the visible task list
</verification>

<success_criteria>
Filter buttons visible for Today/This week/This month, selecting a filter updates displayed tasks
</success_criteria>

<output>
After completion, create `.planning/phases/10-calendar-redesign-my-tasks-filters/10-02-SUMMARY.md`
</output>
