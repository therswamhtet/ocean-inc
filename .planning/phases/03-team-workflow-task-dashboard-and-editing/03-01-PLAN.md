---
phase: 03-team-workflow-task-dashboard-and-editing
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/page.tsx
  - app/login/actions.ts
  - app/team/layout.tsx
  - app/team/sidebar.tsx
  - app/team/page.tsx
autonomous: true
requirements:
  - TEAM-01
  - TEAM-02
must_haves:
  truths:
    - "Team members who sign in land on /team instead of /admin (per D-01)"
    - "Team members see summary cards for total assigned, due today, overdue, and completed"
    - "Team members see only assigned tasks in a due-date-sorted dashboard list with project, client, title, and status dot"
  artifacts:
    - path: "app/team/page.tsx"
      provides: "Team dashboard with metrics and assigned task list"
      min_lines: 80
    - path: "app/team/layout.tsx"
      provides: "Protected team shell"
      contains: "redirect('/login')"
    - path: "app/login/actions.ts"
      provides: "Role-aware post-login redirect"
      contains: "team_member"
  key_links:
    - from: "app/page.tsx"
      to: "/team"
      via: "role-based root redirect"
      pattern: "team_member.*redirect\\('/team'\\)"
    - from: "app/team/page.tsx"
      to: "task_assignments/tasks/projects/clients"
      via: "dashboard query"
      pattern: "task_assignments|projects|clients"
---

<objective>
Create the team route shell and dashboard entry experience.

Purpose: Make team members land in their own workspace and immediately see their workload summary and assigned-task queue.
Output: `/team` layout, sidebar, role-aware redirects, and dashboard page.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-team-workflow-task-dashboard-and-editing/03-CONTEXT.md
@.planning/phases/03-team-workflow-task-dashboard-and-editing/03-RESEARCH.md
@app/page.tsx
@app/login/actions.ts
@app/admin/layout.tsx
@app/admin/sidebar.tsx
@app/admin/page.tsx
@components/ui/status-dot.tsx

<interfaces>
From `components/ui/status-dot.tsx`:
```ts
export function StatusDot({ status, showLabel = false }: { status: 'todo' | 'in_progress' | 'done' | 'overdue'; showLabel?: boolean })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the dedicated team shell and role-aware entry redirects</name>
  <files>app/page.tsx, app/login/actions.ts, app/team/layout.tsx, app/team/sidebar.tsx</files>
  <read_first>
    - app/page.tsx
    - app/login/actions.ts
    - app/admin/layout.tsx
    - app/admin/sidebar.tsx
    - .planning/phases/03-team-workflow-task-dashboard-and-editing/03-CONTEXT.md
  </read_first>
  <action>
    Implement D-01 by updating both `app/page.tsx` and `app/login/actions.ts` to read `user.app_metadata.role` after `auth.getUser()` / `signInWithPassword()`. Redirect admins to `/admin` and team members to `/team`; keep `/login` as the fallback for unauthenticated users.

    Create `app/team/layout.tsx` as a protected server layout that mirrors the admin shell shape but is dedicated to team members, not shared with admin (per D-02 boundary). It must:
    - call `createClient()` and `auth.getUser()`
    - redirect unauthenticated users to `/login`
    - redirect authenticated non-team users to `/admin`
    - render a left sidebar on desktop and sheet-based navigation on mobile
    - reuse the existing sign-out action from `app/login/actions.ts`

    Create `app/team/sidebar.tsx` as a client component with exactly two nav items: `/team` labeled `Dashboard` and `/team/tasks`-scoped navigation label `Tasks`. Use the same active-route helper style as admin, black/white border-only styling, and no extra nav items.
  </action>
  <acceptance_criteria>
    - `app/page.tsx` contains role-aware redirects for both `admin` and `team_member`
    - `app/login/actions.ts` contains role-aware redirects for both `admin` and `team_member`
    - `app/team/layout.tsx` exists and redirects unauthenticated users to `/login`
    - `app/team/layout.tsx` redirects authenticated non-team users away from `/team`
    - `app/team/sidebar.tsx` exists and contains links for `/team` and `/team/tasks`
    - No admin-layout files are repurposed to serve team navigation
  </acceptance_criteria>
  <verify>
    <automated>npx eslint app/page.tsx app/login/actions.ts app/team/layout.tsx app/team/sidebar.tsx && npm run build</automated>
  </verify>
  <done>Signed-in team members route into a dedicated `/team` shell and cannot fall through to the admin workspace.</done>
</task>

<task type="auto">
  <name>Task 2: Build the team dashboard cards and assigned-task list</name>
  <files>app/team/page.tsx</files>
  <read_first>
    - app/team/page.tsx
    - app/admin/page.tsx
    - app/admin/clients/[clientId]/projects/[projectId]/page.tsx
    - components/ui/status-dot.tsx
    - supabase/migrations/001_initial_schema.sql
    - supabase/migrations/002_rls_policies.sql
  </read_first>
  <action>
    Create `app/team/page.tsx` as an async server component that implements TEAM-01 and TEAM-02 using the team-member account's own RLS-scoped view of the data.

    Query the signed-in team member's assigned tasks by joining through `task_assignments` and including `projects(name, client_id, clients(name))`. Sort the visible task list by `due_date` ascending with null due dates last, then `created_at` ascending as a stable fallback. Render:
    - four summary cards labeled exactly `Total Assigned`, `Due Today`, `Overdue`, `Completed`
    - a task list showing `project name`, `client name`, `task title`, and `StatusDot`
    - each task row linked to `/team/tasks/[taskId]`
    - overdue detection using the existing rule `posting_date < today AND status != 'done'`

    Keep this page server-rendered like the admin dashboard. Follow the border-only black/white visual pattern from Phase 2. Do not add filters, search, kanban, or realtime updates.
  </action>
  <acceptance_criteria>
    - `app/team/page.tsx` exists as an async server component
    - the page renders cards titled `Total Assigned`, `Due Today`, `Overdue`, and `Completed`
    - the task list query includes project and client names for each assigned task
    - task rows link to `/team/tasks/[taskId]`
    - the page uses `StatusDot` for task status display
    - overdue logic matches `posting_date < today AND status != 'done'`
    - the task list ordering includes `due_date` as the primary sort
  </acceptance_criteria>
  <verify>
    <automated>npx eslint app/team/page.tsx && npm run build</automated>
  </verify>
  <done>The `/team` dashboard shows the required four workload metrics and a due-date-sorted list of only the signed-in member's assigned tasks.</done>
</task>

</tasks>

<verification>
- Team users route to `/team` from both the site root and post-login flow.
- `/team` renders summary cards and assigned-task rows without exposing unrelated tasks.
- Status display uses the existing animated status-dot system.
</verification>

<success_criteria>
- TEAM-01 and TEAM-02 are fully covered.
- D-01 is implemented in both required redirect locations.
- `/team` is a distinct protected workspace, not an admin variation.
</success_criteria>

<output>
After completion, create `.planning/phases/03-team-workflow-task-dashboard-and-editing/03-01-SUMMARY.md`
</output>
