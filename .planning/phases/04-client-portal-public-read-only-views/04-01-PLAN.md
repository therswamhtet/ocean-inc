---
phase: 04-client-portal-public-read-only-views
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/portal/[slug]/page.tsx
  - components/portal/portal-shell.tsx
  - lib/portal/types.ts
  - lib/portal/queries.ts
  - supabase/migrations/007_client_portal_read_policies.sql
  - __tests__/portal-queries.test.ts
autonomous: true
requirements: [CLIENT-01, CLIENT-02, CLIENT-08, CLIENT-09]
must_haves:
  truths:
    - "Client can open /portal/[slug] without login and either see portal content or a correct not-found/empty state"
    - "Portal entry route always renders dynamically per request"
    - "Portal entry path exposes no mutation behavior"
  artifacts:
    - path: "app/portal/[slug]/page.tsx"
      provides: "Public portal entry with slug validation and force-dynamic rendering"
      exports: ["dynamic", "default"]
    - path: "lib/portal/queries.ts"
      provides: "Slug-scoped fetch for client + active project + tasks"
      exports: ["getPortalDataBySlug"]
    - path: "supabase/migrations/007_client_portal_read_policies.sql"
      provides: "Portal read policy strategy"
      contains: "CREATE POLICY"
  key_links:
    - from: "app/portal/[slug]/page.tsx"
      to: "lib/portal/queries.ts"
      via: "getPortalDataBySlug(slug)"
      pattern: "getPortalDataBySlug"
    - from: "lib/portal/queries.ts"
      to: "public.projects"
      via: "status = 'active' filter"
      pattern: "status.*active"
---

<objective>
Establish the portal foundation: typed slug-scoped data contracts, public route entry, read-only guardrails, and dynamic rendering.

Purpose: Make every downstream portal view implement against a stable contract and security baseline.
Output: Portal route + shell + query layer + policy migration + focused query tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-client-portal-public-read-only-views/04-CONTEXT.md
@.planning/phases/04-client-portal-public-read-only-views/04-RESEARCH.md

<interfaces>
From lib/supabase/server.ts:
```ts
export async function createClient(): Promise<ReturnType<typeof createServerClient>>
```

From components/ui/status-dot.tsx:
```ts
type Status = 'todo' | 'in_progress' | 'done' | 'overdue'
export function StatusDot({ status, showLabel }: { status: Status; showLabel?: boolean })
```

From app/admin/clients/[clientId]/projects/[projectId]/task-view-toggle.tsx:
```ts
export type TaskRow = {
  id: string
  title: string
  caption: string | null
  design_file_path: string | null
  posting_date: string | null
  status: 'todo' | 'in_progress' | 'done'
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define portal contracts and slug-scoped query layer</name>
  <files>lib/portal/types.ts, lib/portal/queries.ts, __tests__/portal-queries.test.ts</files>
  <read_first>lib/supabase/server.ts, app/admin/clients/[clientId]/projects/[projectId]/page.tsx, supabase/migrations/001_initial_schema.sql, .planning/phases/04-client-portal-public-read-only-views/04-CONTEXT.md</read_first>
  <behavior>
    - Valid slug resolves a client + at most one active project + project tasks (per D-03, D-04).
    - Invalid slug returns null result shape and does not throw.
    - Query helper returns typed records (no any) for downstream views.
  </behavior>
  <acceptance_criteria>
    - `lib/portal/types.ts` exports `PortalTaskStatus`, `PortalTask`, `PortalProject`, `PortalClient`, and `PortalData`.
    - `lib/portal/queries.ts` contains `export async function getPortalDataBySlug(slug: string)`.
    - Query includes active-project filter `status = 'active'` (per D-03/D-04).
    - `__tests__/portal-queries.test.ts` exists and contains at least one invalid-slug case and one active-project case.
  </acceptance_criteria>
  <action>Create typed portal contracts first (interface-first) in `lib/portal/types.ts`. Implement `getPortalDataBySlug(slug)` in `lib/portal/queries.ts` using Supabase server client and explicit slug filtering with active project selection (`status = 'active'`, single-record expectation) per D-03/D-04. Add `__tests__/portal-queries.test.ts` that exercises invalid slug behavior and active-project resolution behavior before implementation assertions are finalized.</action>
  <verify>
    <automated>npm run test -- __tests__/portal-queries.test.ts</automated>
  </verify>
  <done>Portal query contracts are exported and tested; downstream plans can consume `PortalTask` without codebase exploration.</done>
</task>

<task type="auto">
  <name>Task 2: Build public portal entry route and shell with dynamic rendering + empty state</name>
  <files>app/portal/[slug]/page.tsx, components/portal/portal-shell.tsx</files>
  <read_first>app/team/page.tsx, app/admin/clients/[clientId]/projects/[projectId]/page.tsx, app/admin/clients/[clientId]/projects/[projectId]/task-view-toggle.tsx, .planning/phases/04-client-portal-public-read-only-views/04-CONTEXT.md</read_first>
  <acceptance_criteria>
    - `app/portal/[slug]/page.tsx` contains `export const dynamic = 'force-dynamic'` (per D-16 / CLIENT-09).
    - Route calls `notFound()` for invalid slug (per D-15).
    - Empty-state copy exactly includes: `No active project yet. Contact your account manager to get started.` (per D-13).
    - `components/portal/portal-shell.tsx` renders top tabs `Kanban | Calendar | Timeline` and defaults to Kanban (per D-05, D-06).
    - Portal files contain no `"use server"` actions or mutation forms (read-only, per D-08).
  </acceptance_criteria>
  <action>Create `app/portal/[slug]/page.tsx` as Server Component public entry with params-based slug validation and `notFound()` fallback (D-15). Add `export const dynamic = 'force-dynamic'` (D-16). Query data via `getPortalDataBySlug`, render empty state text from D-13 when active project is missing, otherwise render `components/portal/portal-shell.tsx` with tabs matching D-05 and Kanban default per D-06. Keep shell read-only (D-08): no mutation handlers, no save/update buttons.</action>
  <verify>
    <automated>npm run lint -- app/portal/[slug]/page.tsx components/portal/portal-shell.tsx</automated>
  </verify>
  <done>Public dynamic portal route is in place, slug-gated, and read-only with stable shell navigation.</done>
</task>

<task type="auto">
  <name>Task 3: Add portal read policy migration for public slug access path</name>
  <files>supabase/migrations/007_client_portal_read_policies.sql</files>
  <read_first>supabase/migrations/002_rls_policies.sql, supabase/migrations/001_initial_schema.sql, .planning/phases/04-client-portal-public-read-only-views/04-CONTEXT.md</read_first>
  <acceptance_criteria>
    - Migration file defines explicit portal read strategy for `clients`, `projects`, and `tasks` (or equivalent secure helper).
    - Migration is read-only (SELECT/EXECUTE path only); no INSERT/UPDATE/DELETE grants.
    - Policy/object names follow existing naming style (`{role}_{action}_{table}`) or document exact exception inline.
    - File includes comments referencing public slug portal scope (per D-17 / CLIENT-08).
  </acceptance_criteria>
  <action>Create `007_client_portal_read_policies.sql` to formalize public portal read path per D-17 while preserving no-mutation guarantees (CLIENT-08). Implement only read/execute-level access for the portal query surface and keep all write paths blocked. Mirror existing migration conventions and add clear comments tying policy scope to `/portal/[slug]` usage.</action>
  <verify>
    <automated>rg "CREATE POLICY|FOR SELECT|portal|slug|anon" supabase/migrations/007_client_portal_read_policies.sql</automated>
  </verify>
  <done>Database migration contains auditable, read-only access rules for portal data retrieval.</done>
</task>

</tasks>

<verification>
Run focused tests/lint for new portal foundation, then run `npm run build` once before summary creation.
</verification>

<success_criteria>
- `/portal/[slug]` route exists and is dynamic + slug-gated.
- Empty state and no-auth public access behavior are implemented.
- Query contracts are reusable for Kanban/Calendar/Timeline plans.
- Portal read policy migration is present and read-only.
</success_criteria>

<output>
After completion, create `.planning/phases/04-client-portal-public-read-only-views/04-01-SUMMARY.md`
</output>
