---
phase: 04-client-portal-public-read-only-views
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - lib/portal/calendar-utils.ts
  - components/portal/calendar-view.tsx
  - __tests__/portal-calendar-utils.test.ts
autonomous: true
requirements: [CLIENT-04]
must_haves:
  truths:
    - "Client can switch between month and week calendar views"
    - "Tasks are plotted by posting date in both calendar modes"
  artifacts:
    - path: "components/portal/calendar-view.tsx"
      provides: "Month/week toggle calendar rendering"
      exports: ["PortalCalendarView"]
    - path: "lib/portal/calendar-utils.ts"
      provides: "Calendar date-grid and task grouping helpers"
      exports: ["buildMonthGrid", "buildWeekGrid", "groupTasksByPostingDate"]
  key_links:
    - from: "components/portal/calendar-view.tsx"
      to: "lib/portal/calendar-utils.ts"
      via: "date grid + grouping helpers"
      pattern: "buildMonthGrid|buildWeekGrid|groupTasksByPostingDate"
---

<objective>
Implement the Calendar view with month-by-default behavior and week/month toggle for posting-date task plotting.

Purpose: Provide date-centric planning visibility for clients beyond Kanban columns.
Output: Calendar utilities + UI component + focused utility tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-client-portal-public-read-only-views/04-CONTEXT.md
@.planning/phases/04-client-portal-public-read-only-views/04-01-PLAN.md

<interfaces>
From lib/portal/types.ts (from 04-01):
```ts
export type PortalTask = {
  id: string
  title: string
  posting_date: string | null
  status: 'todo' | 'in_progress' | 'done'
}
```

From components/ui/status-dot.tsx:
```ts
export function StatusDot({ status }: { status: 'todo' | 'in_progress' | 'done' | 'overdue' })
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create calendar utility layer for month/week plotting</name>
  <files>lib/portal/calendar-utils.ts, __tests__/portal-calendar-utils.test.ts</files>
  <read_first>lib/utils.ts, app/team/page.tsx, .planning/phases/04-client-portal-public-read-only-views/04-CONTEXT.md</read_first>
  <behavior>
    - Month mode returns full month grid with leading/trailing days.
    - Week mode returns exactly 7-day window.
    - Tasks are grouped by posting_date key and ignore null dates.
  </behavior>
  <acceptance_criteria>
    - `calendar-utils.ts` exports `buildMonthGrid`, `buildWeekGrid`, and `groupTasksByPostingDate`.
    - Week builder always returns length 7.
    - Grouping helper maps tasks by `posting_date` (ISO date string key).
    - Test file covers month-grid shape, week length, and null-date handling.
  </acceptance_criteria>
  <action>Implement pure date helpers with `date-fns` only (no new dependency) to support D-09/D-10. Write tests first for expected grid and grouping behavior, then implement helpers to pass. Ensure helper signatures are stable and reusable by the calendar component.</action>
  <verify>
    <automated>npm run test -- __tests__/portal-calendar-utils.test.ts</automated>
  </verify>
  <done>Reusable calendar logic exists with deterministic tested output for week/month plotting.</done>
</task>

<task type="auto">
  <name>Task 2: Build portal Calendar component with month-default toggle UI</name>
  <files>components/portal/calendar-view.tsx</files>
  <read_first>components/admin/task-list.tsx, components/ui/status-dot.tsx, lib/portal/calendar-utils.ts, .planning/phases/04-client-portal-public-read-only-views/04-CONTEXT.md</read_first>
  <acceptance_criteria>
    - Component renders a month/week toggle control with month selected by default (D-09).
    - Month mode and week mode both plot tasks by posting date (D-10 / CLIENT-04).
    - Date cells include task title text and status indicator usage.
    - Component uses `PortalTask` type from `lib/portal/types.ts`.
  </acceptance_criteria>
  <action>Create `PortalCalendarView` using `calendar-utils` for grid generation and per-date task grouping. Default local state to month mode per D-09, include explicit toggle for week mode, and render tasks keyed by posting date with `StatusDot` indicators in both modes (D-10). Keep component read-only and self-contained (no data fetching/mutations).</action>
  <verify>
    <automated>npm run lint -- components/portal/calendar-view.tsx lib/portal/calendar-utils.ts</automated>
  </verify>
  <done>Calendar view is complete, month-first, and plots tasks correctly for both toggle modes.</done>
</task>

</tasks>

<verification>
Run utility tests and lint; verify month default and week toggle behavior from component markup/state logic.
</verification>

<success_criteria>
- Calendar supports week/month toggle with month default.
- Posting-date plotting is deterministic and test-backed.
- No external calendar library added.
</success_criteria>

<output>
After completion, create `.planning/phases/04-client-portal-public-read-only-views/04-03-SUMMARY.md`
</output>
