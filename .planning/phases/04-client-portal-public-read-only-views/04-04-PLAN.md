---
phase: 04-client-portal-public-read-only-views
plan: 04
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - lib/portal/timeline-utils.ts
  - components/portal/timeline-view.tsx
  - __tests__/portal-timeline-utils.test.ts
autonomous: true
requirements: [CLIENT-05]
must_haves:
  truths:
    - "Client can view tasks in month-grouped timeline swimlanes"
    - "Timeline is horizontally scrollable while month labels remain readable"
  artifacts:
    - path: "components/portal/timeline-view.tsx"
      provides: "Month swimlane timeline with horizontal scroll"
      exports: ["PortalTimelineView"]
    - path: "lib/portal/timeline-utils.ts"
      provides: "Month grouping and date-position math for timeline bars"
      exports: ["groupTasksByMonth", "calculateTimelineOffset"]
  key_links:
    - from: "components/portal/timeline-view.tsx"
      to: "lib/portal/timeline-utils.ts"
      via: "grouping + offset helpers"
      pattern: "groupTasksByMonth|calculateTimelineOffset"
---

<objective>
Implement the Timeline view as month-grouped swimlanes with horizontal scrolling task bars.

Purpose: Provide linear, month-cycle visibility aligned with project cadence.
Output: Timeline utilities + timeline component + focused utility tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-client-portal-public-read-only-views/04-CONTEXT.md
@.planning/phases/04-client-portal-public-read-only-views/04-01-PLAN.md

<interfaces>
From lib/portal/types.ts (from 04-01):
```ts
export type PortalTask = {
  id: string
  title: string
  posting_date: string | null
  status: 'todo' | 'in_progress' | 'done'
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create timeline grouping and offset utility helpers</name>
  <files>lib/portal/timeline-utils.ts, __tests__/portal-timeline-utils.test.ts</files>
  <read_first>lib/portal/calendar-utils.ts, .planning/phases/04-client-portal-public-read-only-views/04-CONTEXT.md, .planning/phases/04-client-portal-public-read-only-views/04-RESEARCH.md</read_first>
  <behavior>
    - Tasks are grouped into month buckets from posting_date.
    - Null posting dates are excluded from timeline bars.
    - Offset helper yields deterministic day-based placement inside month lane.
  </behavior>
  <acceptance_criteria>
    - `timeline-utils.ts` exports `groupTasksByMonth` and `calculateTimelineOffset`.
    - Grouping returns stable month keys (e.g., `2026-04`).
    - Test file covers cross-month grouping and offset boundaries (start/end of month).
  </acceptance_criteria>
  <action>Implement pure timeline helpers to satisfy D-11 requirements with no external charting dependency. Write tests for month grouping and bar offset calculations first, then implement helpers so tests pass. Keep helper output directly consumable by component render loops.</action>
  <verify>
    <automated>npm run test -- __tests__/portal-timeline-utils.test.ts</automated>
  </verify>
  <done>Timeline helpers are deterministic, tested, and ready for rendering swimlanes.</done>
</task>

<task type="auto">
  <name>Task 2: Build timeline swimlane component with horizontal scroll and fixed month labels</name>
  <files>components/portal/timeline-view.tsx</files>
  <read_first>components/admin/task-list.tsx, components/ui/status-dot.tsx, lib/portal/timeline-utils.ts, .planning/phases/04-client-portal-public-read-only-views/04-CONTEXT.md</read_first>
  <acceptance_criteria>
    - Component renders month-grouped swimlane rows (D-11).
    - Timeline content area uses horizontal overflow scrolling (D-12).
    - Month labels remain visible while bars render in scrollable track region.
    - Bars represent task placement by posting date and include readable task title text.
  </acceptance_criteria>
  <action>Create `PortalTimelineView` with left-side month labels and a horizontally scrollable lane area per D-11/D-12. Render one lane per grouped month and place task bars by `calculateTimelineOffset` output. Keep styling within project brand constraints (no shadow/gradient; radius <= 8px) and maintain read-only behavior.</action>
  <verify>
    <automated>npm run lint -- components/portal/timeline-view.tsx lib/portal/timeline-utils.ts</automated>
  </verify>
  <done>Timeline view is functional, month-grouped, and horizontally navigable per locked decisions.</done>
</task>

</tasks>

<verification>
Run timeline utility tests and lint; confirm component markup contains month lane and horizontal overflow patterns.
</verification>

<success_criteria>
- Timeline renders month swimlanes from posting-date tasks.
- Horizontal scroll behavior is implemented with fixed labels.
- Utility logic is test-backed and reusable.
</success_criteria>

<output>
After completion, create `.planning/phases/04-client-portal-public-read-only-views/04-04-SUMMARY.md`
</output>
