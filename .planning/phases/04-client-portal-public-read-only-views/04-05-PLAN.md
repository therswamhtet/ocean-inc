---
phase: 04-client-portal-public-read-only-views
plan: 05
type: execute
wave: 3
depends_on: [04-02, 04-03, 04-04]
files_modified:
  - components/portal/task-detail-dialog.tsx
  - components/portal/portal-shell.tsx
  - components/portal/kanban-view.tsx
  - components/portal/calendar-view.tsx
  - components/portal/timeline-view.tsx
  - __tests__/portal-task-dialog.test.tsx
autonomous: true
requirements: [CLIENT-07, CLIENT-10]
must_haves:
  truths:
    - "Clicking any task in Kanban/Calendar/Timeline opens a read-only detail modal"
    - "Modal includes caption copy and design-file signed download behavior"
  artifacts:
    - path: "components/portal/task-detail-dialog.tsx"
      provides: "Read-only task detail modal"
      exports: ["PortalTaskDetailDialog"]
    - path: "components/portal/portal-shell.tsx"
      provides: "Shared modal state wiring across all views"
      exports: ["PortalShell"]
  key_links:
    - from: "components/portal/portal-shell.tsx"
      to: "components/portal/task-detail-dialog.tsx"
      via: "selectedTask state"
      pattern: "selectedTask|setSelectedTask"
    - from: "components/portal/task-detail-dialog.tsx"
      to: "components/admin/design-file-downloader.tsx"
      via: "DesignFileDownloader"
      pattern: "DesignFileDownloader"
---

<objective>
Deliver the cross-view task detail modal and signed-download read surface for the portal.

Purpose: Complete the client read-only interaction loop from task discovery to detail inspection.
Output: Shared dialog component + click wiring in all views + modal test coverage.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-client-portal-public-read-only-views/04-CONTEXT.md
@.planning/phases/02-admin-core-client-project-and-task-management/02-07-SUMMARY.md
@.planning/phases/04-client-portal-public-read-only-views/04-02-PLAN.md
@.planning/phases/04-client-portal-public-read-only-views/04-03-PLAN.md
@.planning/phases/04-client-portal-public-read-only-views/04-04-PLAN.md

<interfaces>
From components/admin/copy-button.tsx:
```ts
type CopyButtonProps = { text: string; label?: string; className?: string }
export default function CopyButton(props: CopyButtonProps)
```

From components/admin/design-file-downloader.tsx:
```ts
type DesignFileDownloaderProps = { filePath: string; fileName: string }
export default function DesignFileDownloader(props: DesignFileDownloaderProps)
```

From components/ui/dialog.tsx:
```ts
export { Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build read-only task detail dialog component</name>
  <files>components/portal/task-detail-dialog.tsx, __tests__/portal-task-dialog.test.tsx</files>
  <read_first>components/ui/dialog.tsx, components/admin/copy-button.tsx, components/admin/design-file-downloader.tsx, components/ui/status-dot.tsx, .planning/phases/04-client-portal-public-read-only-views/04-CONTEXT.md</read_first>
  <behavior>
    - Dialog shows caption, posting date, status dot, and design file download.
    - Caption copy uses existing CopyButton behavior.
    - Dialog has no editable inputs or mutation actions (read-only).
  </behavior>
  <acceptance_criteria>
    - `task-detail-dialog.tsx` imports and uses `Dialog`, `CopyButton`, `DesignFileDownloader`, and `StatusDot`.
    - Component renders fields from D-07 and omits admin-only editing controls per D-08.
    - No `"use server"` or form submission handlers exist in the dialog file.
    - `portal-task-dialog.test.tsx` verifies required content rendering and read-only behavior.
  </acceptance_criteria>
  <action>Create `PortalTaskDetailDialog` using shadcn dialog primitives per D-07. Show only read-only fields: caption (with `CopyButton`), design file download (`DesignFileDownloader` with 60s signed URL behavior via existing component), posting date, and status (`StatusDot`). Exclude briefing/edit/save controls per D-08. Add tests for field presence and absence of editable mutation affordances.</action>
  <verify>
    <automated>npm run test -- __tests__/portal-task-dialog.test.tsx</automated>
  </verify>
  <done>Task detail modal is reusable, read-only, and functionally complete for portal UX.</done>
</task>

<task type="auto">
  <name>Task 2: Wire task-click modal flow across Kanban, Calendar, and Timeline views</name>
  <files>components/portal/portal-shell.tsx, components/portal/kanban-view.tsx, components/portal/calendar-view.tsx, components/portal/timeline-view.tsx</files>
  <read_first>components/portal/portal-shell.tsx, components/portal/kanban-view.tsx, components/portal/calendar-view.tsx, components/portal/timeline-view.tsx, components/portal/task-detail-dialog.tsx</read_first>
  <acceptance_criteria>
    - Shell tracks selected task state and opens/closes `PortalTaskDetailDialog`.
    - Each view exposes/uses a typed `onTaskSelect(task)` callback.
    - Clicking a task from any view opens the same modal with correct task payload.
    - Portal remains read-only (no mutation callbacks introduced).
  </acceptance_criteria>
  <action>Update `PortalShell` to own `selectedTask` + `dialogOpen` state and render `PortalTaskDetailDialog`. Extend Kanban, Calendar, and Timeline component props with a shared `onTaskSelect` callback and wire click targets on task cards/bars/cell items to invoke it. Ensure all wiring remains read-only and does not introduce mutation paths.</action>
  <verify>
    <automated>npm run lint -- components/portal/portal-shell.tsx components/portal/kanban-view.tsx components/portal/calendar-view.tsx components/portal/timeline-view.tsx components/portal/task-detail-dialog.tsx</automated>
  </verify>
  <done>Clients can open a consistent task detail modal from all three portal views with signed-download + copy behavior intact.</done>
</task>

</tasks>

<verification>
Run dialog tests + lint, then run `npm run test && npm run build` for final phase-integrated confidence.
</verification>

<success_criteria>
- Task clicks in all views open one shared modal.
- Modal contains exactly required read-only fields and controls.
- Signed URL download flow is reused via existing downloader component (60s expiry).
</success_criteria>

<output>
After completion, create `.planning/phases/04-client-portal-public-read-only-views/04-05-SUMMARY.md`
</output>
