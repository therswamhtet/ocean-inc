---
phase: "05.1"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "app/admin/mobile-nav.tsx"
  - "app/team/mobile-nav.tsx"
  - "app/admin/layout.tsx"
  - "app/team/layout.tsx"
autonomous: true
requirements: []

must_haves:
  truths:
    - "Mobile hamburger menu appears on the right side on <768px screens"
    - "All 'admin console' text replaced with 'management panel' throughout app"
  artifacts:
    - path: "app/admin/mobile-nav.tsx"
      provides: "Mobile navigation with hamburger on right"
      contains: "SheetContent side=\"right\""
    - path: "app/team/mobile-nav.tsx"
      provides: "Team mobile navigation with hamburger on right"
      contains: "SheetContent side=\"right\""
    - path: "grep_results.txt"
      provides: "Verification that no 'admin console' text remains"
      contains: "0 matches for admin console"
---

<objective>
Fix mobile navigation positioning and terminology consistency.

Purpose: Move hamburger menu from left to right side on mobile, and replace "admin console" with "management panel" throughout the app.
Output: Updated mobile navigation and terminology across admin and team surfaces.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/admin/mobile-nav.tsx
@app/team/mobile-nav.tsx
@app/admin/layout.tsx
@app/team/layout.tsx
@lib/labels.ts
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From components/ui/sheet.tsx:
```typescript
SheetContent side="left" | side="right" | side="top" | side="bottom"
```

From lib/labels.ts:
```typescript
export const LABELS = {
  common: {
    adminPanel: 'Management Panel',
    // ...
  },
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Move admin mobile hamburger to right side</name>
  <files>app/admin/mobile-nav.tsx</files>
  <read_first>
    - app/admin/mobile-nav.tsx (file being modified)
  </read_first>
  <action>
    In app/admin/mobile-nav.tsx:
    1. Change `SheetContent side="left"` to `SheetContent side="right"` on line 44
    2. The hamburger button (Menu icon) is already on the right side of the header — this change makes the slide-out panel open from the right for better mobile UX
  </action>
  <verify>
    <automated>grep "SheetContent side="right"" app/admin/mobile-nav.tsx && ! grep "SheetContent side=\"left\"" app/admin/mobile-nav.tsx</automated>
  </verify>
  <done>Admin mobile hamburger opens from right side</done>
</task>

<task type="auto">
  <name>Task 2: Move team mobile hamburger to right side</name>
  <files>app/team/mobile-nav.tsx</files>
  <read_first>
    - app/team/mobile-nav.tsx (file being modified)
  </read_first>
  <action>
    In app/team/mobile-nav.tsx:
    1. Find the SheetContent with side="left"
    2. Change it to `SheetContent side="right"`
    Note: Check if the team mobile-nav file exists and has the same pattern as admin mobile-nav. If it uses a different structure, update accordingly.
  </action>
  <verify>
    <automated>grep "SheetContent side="right"" app/team/mobile-nav.tsx || echo "File may not exist or no change needed"</automated>
  </verify>
  <done>Team mobile hamburger opens from right side</done>
</task>

<task type="auto">
  <name>Task 3: Replace 'admin console' with 'management panel' throughout app</name>
  <files>app/admin/layout.tsx, app/team/layout.tsx</files>
  <read_first>
    - app/admin/layout.tsx
    - app/team/layout.tsx
    - lib/labels.ts
  </read_first>
  <action>
    1. Search for all instances of "admin console" (case-insensitive) in app/ and components/ directories
    2. Replace each occurrence with "Management Panel" or use LABELS.common.adminPanel which is already set to "Management Panel"
    
    Files to check and update:
    - app/admin/layout.tsx (if it has hardcoded "Admin Console" text)
    - app/team/layout.tsx (if it has hardcoded "Admin Console" text)
    - Any other files containing the string
    
    Use the existing LABELS.common.adminPanel constant where possible to maintain consistency.
  </action>
  <verify>
    <automated>! grep -ri "admin console" app/ components/ 2>/dev/null && grep "LABELS.common.adminPanel" app/admin/layout.tsx</automated>
  </verify>
  <done>No 'admin console' text remains; management panel terminology used consistently</done>
</task>

</tasks>

<verification>
npm run lint passes with no new errors related to the changes made.
grep -ri "admin console" app/ components/ returns 0 matches
grep "SheetContent side="right"" app/admin/mobile-nav.tsx returns 1 match
</verification>

<success_criteria>
Mobile hamburger menus open from the right side on both admin and team layouts. All instances of "admin console" replaced with "management panel" throughout the application.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-ui-ux-polish-phase-for-urgent-refinement-work-discovered-mid-milestone/05.1-01-SUMMARY.md`
</output>
