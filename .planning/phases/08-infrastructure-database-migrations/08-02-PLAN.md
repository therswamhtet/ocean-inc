---
phase: 08-infrastructure-database-migrations
plan: "02"
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - app/admin/clients/actions.ts
  - app/admin/clients/client-card.tsx
  - lib/types/database.ts
autonomous: true
requirements:
  - CLIENT-11
  - CLIENT-12
must_haves:
  truths:
    - "Client description is optional textarea, max 200 chars, added to client create form"
    - "Client is_active toggle on client-card and client detail page, blocked clients muted with badge"
    - "getClientsAction and getAdminDataAction must SELECT description and is_active columns"
  artifacts:
    - path: "app/admin/clients/client-card.tsx"
      provides: "Toggle switch for is_active, muted styling for blocked, blocked badge"
      min_lines: 100
    - path: "app/admin/clients/actions.ts"
      provides: "Updated server actions with description and is_active"
      min_lines: 50
---

<objective>
Wire client description and is_active fields into admin create flow, client list display, and toggle

Purpose: Admins can add descriptions, block/unblock clients via toggle, blocked clients visually distinct
Output: Updated create form, client-card with toggle, server actions
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/phases/08-infrastructure-database-migrations/08-CONTEXT.md
@supabase/migrations/012_v11_columns.sql
@app/admin/clients/actions.ts
@app/admin/clients/client-card.tsx
@app/admin/clients/page.tsx
@app/admin/clients/[clientId]/actions.ts
</execution_context>

<context>
## Key Decisions (from CONTEXT.md)
- D-01 through D-04: Client description as optional textarea (max ~200 chars) in create form, editable on client detail page
- D-05 through D-07: Toggle/switch for blocked status on client-card AND client detail page; muted styling for blocked
- D-03: Description editable on client detail page after creation
- D-21: toggleClientStatusAction — server action to toggle is_active
</id>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update createClientAction to include description; update getClientsAction and getAdminDataAction to select description + is_active</name>
  <files>app/admin/clients/actions.ts</files>
  <action>
    Update `app/admin/clients/actions.ts`:

    1. **createClientAction**: Add `description` field extraction from FormData:
       - `const description = String(formData.get('description') ?? '').trim()` (do NOT trim null to empty, allow null)
       - Actually per D-01, description is optional and nullable. Store empty string as NULL:
         `const descriptionRaw = String(formData.get('description') ?? '').trim()`
         `const description = descriptionRaw.length > 0 ? descriptionRaw : null`
       - Add `description` to the insert payload alongside `name`, `slug`, `color`

    2. **getClientsAction**: Update select and type:
       - Type: `type Client = { id: string; name: string; color: string; description: string | null; is_active: boolean }`
       - Select: `select('id, name, color, description, is_active')`
       - Return both new fields to caller

    3. **getAdminDataAction**: Update select:
       - `select('id, name, color, description, is_active')`
       - Client type updated

    4. **toggleClientStatusAction** — new server action:
       - Takes `clientId: string`
       - Authenticates user, uses serviceRoleClient
       - Flips `is_active`: first SELECT current value, then UPDATE SET is_active = NOT is_active
       - Returns { success: true } or { success: false, error: string }
       - Revalidates `/admin/clients`

    Follow existing patterns: getUser() check, service role client, redirect on error.
  </action>
  <read_first>
    - app/admin/clients/actions.ts (current code for createClientAction and getClientsAction)
    - lib/supabase/server.ts (service role client pattern)
  </read_first>
  <acceptance_criteria>
    - createClientAction extracts description from FormData, stores null when empty
    - getClientsAction selects description and is_active columns
    - getClientsAction Client type includes description: string | null and is_active: boolean
    - getAdminDataAction selects description and is_active
    - toggleClientStatusAction exists, authenticates, flips is_active, returns success/error
    - toggleClientStatusAction revalidates /admin/clients
  </acceptance_criteria>
  <verify>
    <automated>
      grep -c "description" app/admin/clients/actions.ts
      grep -c "is_active" app/admin/clients/actions.ts
      grep -c "toggleClientStatusAction" app/admin/clients/actions.ts
    </automated>
  </verify>
  <done>Server actions updated: createClientAction with description, getClientsAction/adminDataAction with description+is_active, new toggleClientStatusAction</done>
</task>

<task type="auto">
  <name>Task 2: Add description textarea to client create dialog and is_active toggle to client-card</name>
  <files>app/admin/clients/client-card.tsx, components/admin/create-client-dialog.tsx</files>
  <action>
    Update `app/admin/clients/client-card.tsx`:

    1. Update the component's props type to include `isActive: boolean` and `description: string | null`
    2. **Blocked client visual treatment** (D-07):
       - When `isActive === false`, apply muted styling: `opacity-60` on the card
       - Show a "Blocked" badge in red next to the client name
       - The "View portal" link is hidden for blocked clients
    3. **Toggle switch** for is_active status (D-06):
       - Add a shadcn `Switch` component near the delete button on the card
       - Label: "Blocked" — when switch is on, client is blocked (is_active = false)
       - Actually use clearer UX: label toggle as "Active", toggle on = active, toggle off = blocked
       - On toggle change, call `toggleClientStatusAction` via a form submission
       - Add optimistic UI update after toggle
    4. **Description display**: Show description as truncated text below the client name if it exists

    The create dialog may already handle client creation. Update it to include:
    1. An optional textarea for description (max 200 chars)
    2. Add `name="description"` so createClientAction reads it

    If there is no separate create-dialog component, the create form lives in the page itself. Check `app/admin/clients/page.tsx` for where the create form lives and update there.
  </action>
  <read_first>
    - app/admin/clients/client-card.tsx (current component, understand props and structure)
    - app/admin/clients/page.tsx (check if create form is inline or separate dialog)
    - components/admin/create-dialog.tsx (if exists)
  </read_first>
  <acceptance_criteria>
    - Client-card props include isActive and description
    - Blocked clients have muted/appearance-60 opacity and a red "Blocked" badge
    - Toggle switch calls toggleClientStatusAction on change
    - Create dialog/form has optional description textarea with maxLength 200
    - "View portal" link hidden for blocked clients
  </acceptance_criteria>
  <verify>
    <automated>
      grep -c "description" app/admin/clients/client-card.tsx
      grep -c "is_active\|isActive" app/admin/clients/client-card.tsx
      grep -c "Switch" app/admin/clients/client-card.tsx
      grep -c "description" app/admin/clients/page.tsx 2>/dev/null || echo "CHECK_DIALOG"
    </automated>
  </verify>
  <done>Client-card updated: description display, blocked indicator (muted + red badge), is_active toggle switch, create dialog has optional description textarea</done>
</task>

<task type="auto">
  <name>Task 3: Update client detail page to edit description and toggle is_active</name>
  <files>
    app/admin/clients/[clientId]/actions.ts
    app/admin/clients/[clientId]/page.tsx
    app/admin/clients/[clientId]/task-edit-form.tsx
  </files>
  <action>
    Update `app/admin/clients/[clientId]/actions.ts`:

    1. **updateClientAction**: Add support for updating `description`:
       - Extract description from FormData
       - Store as null when empty
       - UPDATE clients SET description = ? WHERE id = ?

    2. **updateClientDescriptionAction**: Add a dedicated action (or reuse existing update pattern)
       - Takes client id and description value
       - Updates only the description field
       - Revalidates the client detail page

    The detail page likely has an editable form. Find where the client name is displayed and:
    3. Add description field (editable textarea, max 200 chars, current value as placeholder/default)
    4. Add is_active toggle switch to detail page (same as on client-card, uses toggleClientStatusAction from actions.ts)

    Do NOT use `app/` imports in `components/`. If the detail page uses a shared component, put the toggle there. Otherwise use it in the page.
  </action>
  <read_first>
    - app/admin/clients/[clientId]/actions.ts (existing update pattern)
    - app/admin/clients/[clientId]/page.tsx (current detail layout)
    - app/admin/clients/[clientId]/task-edit-form.tsx (if it has form patterns to follow)
  </read_first>
  <acceptance_criteria>
    - Client detail page has editable description textarea
    - Client detail page has is_active toggle
    - updateClientAction handles description field
    - Both changes revalidate the appropriate path
  </acceptance_criteria>
  <verify>
    <automated>
      grep -c "description" app/admin/clients/[clientId]/actions.ts
      grep -c "description" app/admin/clients/[clientId]/page.tsx 2>/dev/null || echo "CHECK_DETAIL"
    </automated>
  </verify>
  <done>Client detail page updated with description editing and is_active toggle</done>
</task>

</tasks>

<verification>
- createClientAction stores description from FormData (null when empty)
- getClientsAction returns description and is_active in Client type
- getAdminDataAction includes description and is_active in select
- toggleClientStatusAction flips is_active for a client
- Client-card shows blocked badge and muted styling when is_active = false
- Client-card has toggle switch for is_active status
- Create dialog/form has optional description textarea (max 200 chars, maxLength attribute)
- Client detail page has editable description and is_active toggle
- "View portal" link hidden on blocked clients
- Database types updated if there is a shared types file
</verification>

<success_criteria>
- Admin can create a client with optional description (CLIENT-11)
- Admin can block/unblock clients via toggle on client-card and detail page (CLIENT-12)
- Blocked clients visually distinct (muted, red badge) (CLIENT-12)
- Client description editable on detail page (D-03)
- All server actions select new columns (D-04)
</success_criteria>
