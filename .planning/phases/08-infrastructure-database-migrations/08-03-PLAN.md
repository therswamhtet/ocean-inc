---
phase: 08-infrastructure-database-migrations
plan: "03"
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - lib/portal/queries.ts
  - app/portal/[slug]/page.tsx
  - app/portal/[slug]/layout.tsx
autonomous: true
requirements:
  - CLIENT-13
must_haves:
  truths:
    - "Blocked clients (is_active = false) return null from portal queries — 404-style"
    - "Portal behaves as if the client doesn't exist when blocked"
    - "getPortalDataBySlug must filter by is_active = true"
  artifacts:
    - path: "lib/portal/queries.ts"
      provides: "Updated portal query filtering by is_active = true"
      min_lines: 5
---

<objective>
Make portal queries filter out blocked clients so they appear as 404

Purpose: Blocked clients should behave as if they don't exist in the portal (D-08, D-09)
Output: Updated getPortalDataBySlug with is_active filter, portal page shows 404 for blocked
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/phases/08-infrastructure-database-migrations/08-CONTEXT.md
@lib/portal/queries.ts
@app/portal/[slug]/page.tsx
@app/portal/[slug]/layout.tsx
</execution_context>

<context>
## Key Decisions (from CONTEXT.md)
- D-08: Blocked client portal returns 403-style "blocked" page — behaves as if the client doesn't exist
- D-09: Portal queries must filter by is_active = true so blocked clients return no data
- Portal should NOT show "you're blocked" — just look like the page doesn't exist
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update getPortalDataBySlug to filter by is_active = true</name>
  <files>lib/portal/queries.ts</files>
  <action>
    Update `lib/portal/queries.ts`:

    1. In `getPortalDataBySlug`, add a `SELECT is_active` and check, OR add `.eq('is_active', true)` to the client lookup query:
       - Original: `.select('id, name, slug, color').eq('slug', normalizedSlug)`
       - Updated: `.select('id, name, slug, color, is_active').eq('slug', normalizedSlug).eq('is_active', true)`
       - This means blocked clients will return null from the query entirely

    2. Update `ClientRow` type to include `is_active: boolean`

    3. The function already returns `null` when client is not found — this is the correct behavior for blocked clients (D-08)

    4. The caller (`app/portal/[slug]/page.tsx`) already handles null by rendering 404/Not Found — verify this is the case.
  </action>
  <read_first>
    - lib/portal/queries.ts (current getPortalDataBySlug implementation)
    - app/portal/[slug]/page.tsx (how it handles null client)
  </read_first>
  <acceptance_criteria>
    - Client query includes `.eq('is_active', true)` filter
    - ClientRow type includes is_active: boolean
    - Blocked clients return null from getPortalDataBySlug
    - Portal page renders 404-style not found for blocked clients
  </acceptance_criteria>
  <verify>
    <automated>
      grep -c "is_active" lib/portal/queries.ts
      grep -c "is_active" app/portal/[slug]/page.tsx 2>/dev/null || echo "CHECK_PAGE"
    </automated>
  </verify>
  <done>Portal query filters by is_active = true; blocked clients return null; portal shows 404</done>
</task>

</tasks>

<verification>
- getPortalDataBySlug includes .eq('is_active', true) on the client query
- ClientRow type includes is_active: boolean
- Blocked clients return null from getPortalDataBySlug
- Portal page shows not-found/404 for blocked clients
- No "you are blocked" message — behaves as if page doesn't exist (D-08)
</verification>

<success_criteria>
- Portal for blocked clients returns null (D-09)
- Portal renders 404-style page for blocked clients (D-08)
- Existing functionality for active clients unchanged
- No RLS policy changes needed (D-17)
</success_criteria>
