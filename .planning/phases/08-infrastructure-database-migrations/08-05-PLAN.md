---
phase: 08-infrastructure-database-migrations
plan: "05"
type: execute
wave: 5
depends_on: ["08-01", "08-04"]
files_modified:
  - app/admin/page.tsx
  - components/admin/task-list.tsx
  - app/admin/tasks/all-tasks.tsx
  - app/admin/tasks/page.tsx
  - app/admin/clients/[clientId]/projects/[projectId]/page.tsx
  - app/admin/clients/[clientId]/projects/[projectId]/task-view-toggle.tsx
  - app/admin/clients/[clientId]/projects/[projectId]/tasks/[taskId]/page.tsx
  - components/admin/task-create-form.tsx
  - components/admin/quick-task-dialog.tsx
  - components/admin/dashboard-inner.tsx
autonomous: true
requirements:
  - USER-02
must_haves:
  truths:
    - "All task assignment displays show @username instead of real name (D-13)"
    - "Assignment dropdowns in task create/edit forms show @username for selecting assignees"
    - "Username displayed with @ prefix to make it clear it is the @handle"
    - "When username is NULL (existing members not yet set), fall back to name or show 'Unset'"
  artifacts:
    - path: "app/admin/page.tsx"
      provides: "Select team_members.username instead of name in assignment query"
      min_lines: 1
    - path: "components/admin/task-list.tsx"
      provides: "Display @username for assignee column"
      min_lines: 1
---

<objective>
Switch all task assignment displays from team_members.name to team_members.username

Purpose: Task assignments show clean @username identifiers (D-13, social-media style)
Output: Updated assignment queries and displays across all surfaces
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/phases/08-infrastructure-database-migrations/08-CONTEXT.md
@supabase/migrations/012_v11_columns.sql
</execution_context>

<context>
## Key Decisions (from CONTEXT.md)
- D-13: Task assignment displays show **@username only** (not real name) — e.g., in Kanban cards, task detail, all-tasks list
- Username is nullable for existing members — need fallback to name when NULL
- Display format: @username (with @ prefix to indicate it is a handle)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update all assignment queries to select username from team_members</name>
  <files>
    app/admin/page.tsx
    app/admin/tasks/page.tsx
    app/admin/clients/[clientId]/projects/[projectId]/page.tsx
    app/admin/clients/[clientId]/projects/[projectId]/tasks/[taskId]/page.tsx
  </files>
  <action>
    Update every `team_members(name, ...)` select to also include `username`:

    1. **app/admin/page.tsx** (line ~74): `.select('task_id, team_member_id, team_members(name), ...')`
       Change to: `team_members(name, username)`
       Rename extracted field: `assignee_name: raw.team_members?.username ?? raw.team_members?.name ?? null`

    2. **app/admin/tasks/page.tsx** (line ~69): `const assignee = assignments?.[0]?.team_members?.name`
       Change to: `const assignee = assignments?.[0]?.team_members?.username ?? assignments?.[0]?.team_members?.name`

    3. **app/admin/clients/[clientId]/projects/[projectId]/page.tsx** (line ~72): `.task_assignments?.[0]?.team_members?.name`
       Change to: `.task_assignments?.[0]?.team_members?.username ?? .task_assignments?.[0]?.team_members?.name`

    4. **app/admin/clients/[clientId]/projects/[projectId]/tasks/[taskId]/page.tsx** (line ~74): `.select('team_member_id, team_members(name, email)')`
       Change to: `.select('team_member_id, team_members(name, email, username)')`
       Update any display of assignee to use username first

    For each file, update the select query to include `username` from team_members, then update the display code to use `username ?? name` where the assignment name is shown.

    All queries that currently select `team_members(name)` or `team_members(name, email)` must add `username` to their field list.
  </action>
  <read_first>
    - app/admin/page.tsx (task assignments query)
    - app/admin/tasks/page.tsx (task to row mapping with assignee)
    - app/admin/clients/[clientId]/projects/[projectId]/page.tsx (task normalization with assignee)
    - app/admin/clients/[clientId]/projects/[projectId]/tasks/[taskId]/page.tsx (assignment query)
  </read_first>
  <acceptance_criteria>
    - All task assignment queries include team_members.username
    - Display code uses username ?? name as fallback chain
    - No query returns only name when username column is available
    - Assignee type includes optional username field
  </acceptance_criteria>
  <verify>
    <automated>
      grep -rn "team_members.*username" app/admin/ --include="*.tsx" --include="*.ts" | head -10
    </automated>
  </verify>
  <done>All assignment queries updated to select team_members.username; display code uses username first with name fallback</done>
</task>

<task type="auto">
  <name>Task 2: Update task create and edit forms to use username in assignment dropdowns</name>
  <files>
    components/admin/task-create-form.tsx
    components/admin/quick-task-dialog.tsx
    app/admin/clients/[clientId]/projects/[projectId]/tasks/[taskId]/task-edit-form.tsx
  </files>
  <action>
    Update assignment dropdowns in task creation and editing forms:

    1. Wherever `getTeamMembersAction` or direct team_members query results are used to populate assignment dropdowns:
       - The select/combobox options should show `@username` if available, falling back to `name`
       - Format: `@{member.username ?? member.name}`

    2. **team_members fetch**: Where team members are loaded for assignment, add `username` to the select if not already present.

    3. Display format in dropdown: Show `@username` as the primary text, with the real name as secondary/subdued text (only when username exists). When username is NULL, show just the name.

    4. **task-edit-form.tsx**: The existing assignment display/editing should show the current assignee's username and allow switching to other members by their username.
  </action>
  <read_first>
    - components/admin/task-create-form.tsx (assignment dropdown)
    - components/admin/quick-task-dialog.tsx (assignment UI)
    - app/admin/clients/[clientId]/projects/[projectId]/tasks/[taskId]/task-edit-form.tsx (assignment editing)
    - app/admin/clients/[clientId]/actions.ts (getTeamMembersAction or equivalent)
  </read_first>
  <acceptance_criteria>
    - Assignment dropdowns in task-create-form show @username for assignees
    - Quick-task-dialog shows @username for assignees
    - Task-edit-form shows @username for current and available assignments
    - team_members query in each file includes username field
    - Fallback to name when username is NULL
  </acceptance_criteria>
  <verify>
    <automated>
      grep -rn "username" components/admin/task-create-form.tsx
      grep -rn "username" components/admin/quick-task-dialog.tsx
      grep -rn "username" app/admin/clients/*/projects/*/tasks/*/task-edit-form.tsx 2>/dev/null | head -5
    </automated>
  </verify>
  <done>Assignment dropdowns show @username (with name fallback) in task-create-form, quick-task-dialog, and task-edit-form</done>
</task>

<task type="auto">
  <name>Task 3: Update all UI surfaces displaying assignee names</name>
  <files>
    components/admin/task-list.tsx
    app/admin/tasks/all-tasks.tsx
    app/admin/clients/[clientId]/projects/[clientId]/task-view-toggle.tsx
    components/admin/dashboard-inner.tsx
  </files>
  <action>
    Update all assignment name displays to show @username:

    1. **components/admin/task-list.tsx** (lines ~93, ~161):
       - `task.assigned_to_name` should become `task.assigned_to_username ?? task.assigned_to_name`
       - Display format: `@${username}` when username exists, just name when not

    2. **app/admin/tasks/all-tasks.tsx** (lines ~107, ~221):
       - `task.assigned_to_name ?? 'Unassigned'` becomes:
       - If username: `@${username}`
       - Else if name: `name` (no @ prefix for name fallback since it is not a handle)
       - Else: `'Unassigned'`

    3. **app/admin/clients/[clientId]/projects/[projectId]/task-view-toggle.tsx**:
       - Update type to include `assigned_to_username`
       - Update display accordingly

    4. **components/admin/dashboard-inner.tsx**:
       - Update any assignee name display on dashboard to use username

    Display convention:
    - When username is set: show `@{username}` (e.g., @johndoe)
    - When username is NULL but name exists: show just the name (e.g., John Doe)
    - When both are NULL: show '—' or 'Unassigned'
    - The @ prefix is only used with username, not with the name fallback
  </action>
  <read_first>
    - components/admin/task-list.tsx (assignee display locations)
    - app/admin/tasks/all-tasks.tsx (all tasks view with assignee chip)
    - app/admin/clients/[clientId]/projects/[projectId]/task-view-toggle.tsx (task type and kanban display)
    - components/admin/dashboard-inner.tsx (dashboard task list with assignees)
    - app/admin/page.tsx (dashboard task row construction)
  </read_first>
  <acceptance_criteria>
    - task-list.tsx displays @username for assignees
    - all-tasks.tsx displays @username for assignees
    - task-view-toggle.tsx displays @username for assignees
    - dashboard-inner.tsx displays @username for assignees
    - All displays fall back to name when username is NULL
    - @ prefix only used when username is available
  </acceptance_criteria>
  <verify>
    <automated>
      grep -rn "assigned_to_username\|username" components/admin/task-list.tsx
      grep -rn "username" app/admin/tasks/all-tasks.tsx
      grep -rn "username" components/admin/dashboard-inner.tsx
      grep -rn "username" app/admin/clients/*/projects/*/task-view-toggle.tsx
    </automated>
  </verify>
  <done>All assignment displays updated to show @username with name fallback in task-list, all-tasks, task-view-toggle, and dashboard-inner</done>
</task>

</tasks>

<verification>
- All task assignment queries select team_members(username, name) from the database
- Display code uses username ?? name fallback chain everywhere
- Assignment dropdowns show @username for members
- Task list, all-tasks view, kanban, and dashboard show @username for assignable members
- When username is NULL, fallback to name display (no @ prefix)
- @ prefix only used when member has a set username
- task-edit-form shows username for current and available assignments
- No remaining displays of team_members.name in assignment contexts
</verification>

<success_criteria>
- Task assignments display @username only (D-13, USER-02)
- Assignment dropdowns show @username for member selection
- Fallback to name for members without username set (D-14)
- Consistent display convention: @username with @ prefix, name without
</success_criteria>
