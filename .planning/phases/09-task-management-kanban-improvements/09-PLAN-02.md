---
phase: 09
plan: 02
wave: 1
depends_on: []
files_modified:
  - components/admin/task-detail-dialog.tsx
  - components/admin/kanban-card.tsx
  - lib/supabase/storage.ts
autonomous: true
requirements_addressed:
  - TASK-04
  - TASK-05
---

<objective>
Create a redesigned task details dialog component and fix the image previewer to load correctly on page reload with proper preview sizes.
</objective>

<research_context>
This plan covers:
- TASK-04: Task details dialog/drawer redesigned with modern aesthetic
- TASK-05: Image previewer fixed — loads correctly on page reload, uses proper preview mode

The dialog should display all task fields and be reusable across list view and kanban. The image previewer needs to use Supabase Storage signed URLs with appropriate size parameters.
</research_context>

## Tasks

<task>
<read_first>
- components/admin/kanban-card.tsx
- components/portal/task-detail-dialog.tsx
- components/ui/dialog.tsx
- app/admin/clients/[clientId]/projects/[projectId]/task-view-toggle.tsx
</read_first>

<action>
Create a new reusable task detail dialog component at components/admin/task-detail-dialog.tsx:

1. Create TaskDetailDialog component with props:
   - task: TaskRow (full task object)
   - open: boolean
   - onOpenChange: (open: boolean) => void
   - onEdit?: () => void (optional — for switching to edit mode)

2. Display ALL task fields in a read-only view:
   - Title (large, prominent)
   - Status (with colored StatusDot)
   - Assignee (@username or name)
   - Posting Date and Posting Time (formatted together)
   - Due Date and Deadline
   - Briefing (with URL rendering as links)
   - Caption (with Copy button)
   - Design file (with preview/download)

3. Design: Clean layout with clear sections, proper spacing, following existing UI patterns

4. Add a "Copy" button for caption using navigator.clipboard.writeText

5. Add "Download" button for design file that uses signed URL

6. Include close button (X) in top-right corner

The dialog is a display-only view — edit mode can be handled by the parent component switching to edit form.
</action>

<acceptance_criteria>
- TaskDetailDialog component created at components/admin/task-detail-dialog.tsx
- Dialog displays all task fields: title, status, assignee, posting_date, posting_time, due_date, deadline, briefing, caption, design_file
- Caption has working Copy button
- Design file has Download button with signed URL
- Briefing text has clickable links (URLs rendered as anchor tags)
- Close button works to dismiss dialog
- Dialog follows existing UI patterns (colors, spacing, typography)
</acceptance_criteria>
</task>

<task>
<read_first>
- lib/supabase/storage.ts (if exists)
- lib/supabase/server.ts
- components/admin/kanban-card.tsx
</read_first>

<action>
Fix the image previewer to load correctly on page reload and use proper preview sizes:

1. Find the image preview component used in task details — likely uses design_file_path from Supabase Storage

2. For Supabase Storage, images can be resized using URL parameters:
   - Original: `https://[bucket].supabase.co/storage/v1/object/public/[path]`
   - Preview: Append `?width=800&height=600&resize=contain` for thumbnail/preview

3. Create or update image preview component that:
   - Takes design_file_path and displays a properly sized preview
   - Uses Supabase Storage signed URL with size parameters
   - Shows placeholder when no design file exists
   - Handles both images and non-image files (show icon for non-images)

4. For the "loads correctly on page reload" issue:
   - Ensure the image URL is stable (use storage.downloadUrl, not a one-time URL)
   - The preview should render immediately without needing page reload
   - Use Next.js Image component with proper loader for Supabase

5. If using Next.js Image, configure remote patterns for supabase.co

The preview size should be appropriate for the dialog/card context (not full resolution).
</action>

<acceptance_criteria>
- Image preview uses properly sized preview URL (not full resolution)
- Preview loads immediately when dialog/card renders
- Preview persists correctly on page reload
- Non-image files show appropriate icon instead of broken image
- No console errors from image loading failures
</acceptance_criteria>
</task>

<task>
<read_first>
- components/admin/kanban-card.tsx
- components/admin/task-detail-dialog.tsx (to be created)
</read_first>

<action>
Integrate the task detail dialog into the Kanban card click handler:

1. In kanban-card.tsx, on card click:
   - Instead of just opening inline edit dialog (PLAN-01)
   - Open the TaskDetailDialog in "view" mode first
   - Provide an "Edit" button in the dialog that switches to edit mode
   - OR directly open inline edit dialog if the design prefers immediate editing

2. Update the card click handler to:
   - Prevent default navigation
   - Open the task detail dialog
   - Pass full task object to the dialog

3. Ensure the dialog has proper z-index and overlays the board correctly

4. When dialog closes, board should still be in correct state

This is an alternative approach to the inline edit dialog — using a detail view with edit capability.
</action>

<acceptance_criteria>
- Clicking Kanban card opens task detail dialog
- Dialog shows all task fields in read-only mode
- Dialog has edit capability to switch to edit mode
- Dialog overlays board without navigating away
- Closing dialog returns focus to board
</acceptance_criteria>
</task>

## Verification

<must_haves>
- [ ] TaskDetailDialog component exists and displays all task fields
- [ ] Image preview uses properly sized thumbnails (not full resolution)
- [ ] Image preview loads correctly on page reload
- [ ] Kanban card click opens task detail dialog instead of navigating
- [ ] Caption has working Copy button
- [ ] Design file has Download button with signed URL
</must_haves>

<verification_commands>
- npm run build  # Verify no type errors
- Open browser to kanban view, click a card, verify dialog opens
- Verify image preview loads without full-resolution fetch
</verification_commands>
