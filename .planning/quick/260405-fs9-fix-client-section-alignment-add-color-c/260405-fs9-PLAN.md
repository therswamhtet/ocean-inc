---
phase: quick
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - components/portal/portal-shell.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Portal shell shows only Kanban and Calendar tabs"
    - "Kanban is the default selected tab on page load"
  artifacts:
    - path: "components/portal/portal-shell.tsx"
      provides: "Tab navigation for portal views"
---

<objective>
Remove Timeline from the portal shell tab navigation and verify Kanban is set as default.

Purpose: Simplify the portal UI by removing the timeline view option.
Output: Portal shell with only Kanban and Calendar tabs, Kanban defaulting on load.
</objective>

<context>
@components/portal/portal-shell.tsx
</context>

<interfaces>
<!-- Key state and imports from portal-shell.tsx -->
```typescript
// Current state (line 20):
const [activeTab, setActiveTab] = useState<PortalTab>('Kanban')

// Current tab labels (line 16):
const tabLabels = ['Kanban', 'Calendar', 'Timeline'] as const

// Current imports (line 8):
import { PortalTimelineView } from '@/components/portal/timeline-view'
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Remove Timeline tab from portal shell</name>
  <files>components/portal/portal-shell.tsx</files>
  <action>
    1. Remove `PortalTimelineView` from the import statement on line 8
    2. Remove 'Timeline' from the `tabLabels` array on line 16, keeping only `['Kanban', 'Calendar'] as const`
    3. Replace the ternary/else branch that renders PortalTimelineView (lines 63-64) with just the Calendar view, since Timeline will no longer be an option

    After changes:
    - tabLabels should be: `['Kanban', 'Calendar'] as const`
    - Imports should not include PortalTimelineView
    - The conditional rendering should handle: `activeTab === 'Kanban'` -> Kanban view, else -> Calendar view
  </action>
  <verify>grep -n "Timeline\|tabLabels" components/portal/portal-shell.tsx returns no Timeline references</verify>
  <done>Portal shell has only Kanban and Calendar tabs, Kanban is default</done>
</task>

</tasks>

<verification>
- grep "Timeline" portal-shell.tsx returns no matches
- tabLabels contains only 'Kanban' and 'Calendar'
- Kanban is still the default selected tab (line 20 useState)
</verification>

<success_criteria>
Portal shell shows 2 tabs (Kanban, Calendar), Kanban is selected by default, no Timeline imports or references remain.
</success_criteria>

<output>
After completion, create `.planning/quick/260405-fs9-fix-client-section-alignment-add-color-c/260405-fs9-SUMMARY.md`
</output>
