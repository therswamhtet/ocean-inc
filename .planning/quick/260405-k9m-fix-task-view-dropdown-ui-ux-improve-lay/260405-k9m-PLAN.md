---
phase: 260405-k9m-fix-task-view-dropdown-ui-ux-improve-lay
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/admin/tasks/all-tasks.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - Task detail panel has organized, compact layout with clear visual hierarchy
    - Status badge is positioned consistently in the header
    - Label/value pairs are aligned and easy to scan
    - Field grouping maximizes horizontal space, reducing vertical bloat
  artifacts:
    - path: "app/admin/tasks/all-tasks.tsx"
      provides: "Refactored TaskDetailPanel with improved UI/UX"
      min_lines: 100
  key_links:
    - from: "TaskDetailPanel component"
      to: "existing LABELS and helper functions"
      via: "unchanged imports and utilities"
      pattern: "LABELS.common.assignedTo, LABELS.task.*"
---

<objective>
Refactor TaskDetailPanel component to fix UI/UX issues: disorganized information layout, excessive vertical space usage, misaligned status badge, and unclear visual hierarchy.

Purpose: Make task details more scannable and compact while maintaining readability
Output: Improved TaskDetailPanel with better organization, 2-column layout for fields, and clearer label/value alignment
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
Current TaskDetailPanel issues:
1. Fields stacked vertically with `space-y-3` — wastes vertical space
2. Each field row uses `flex items-center gap-2` with icon + label + value — creates visual noise
3. Status badge is pushed to the right with `justify-between` — inconsistent with collapsed row layout
4. Due Date & Deadline use `grid grid-cols-2 gap-3` but positioned at bottom — breaks flow
5. Labels use uppercase tracking but lack visual separation from values

Target improvements:
- Compact 2-column grid layout for single-line fields (Assigned, Posting Date, Content Plan)
- Group Due Date & Deadline together in a dedicated row with clear labeling
- Better header alignment with status badge
- Clearer visual hierarchy with consistent label/value spacing
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor TaskDetailPanel layout and organization</name>
  <files>app/admin/tasks/all-tasks.tsx</files>
  <action>
    Rewrite the TaskDetailPanel component (lines 55-163) to fix all UI/UX issues:

    1. **Header Section (lines 65-90):**
       - Change from `justify-between` to `gap-4` for consistent spacing
       - Wrap status badge in a flex container that stays inline with title
       - Keep client/project info on second line with proper truncation

    2. **Detail Fields Section (lines 92-159):**
       - Replace `space-y-3` vertical stack with compact grid layout
       - Create two-column grid for single-line fields:
         - Column 1: Assigned To + Posting Date
         - Column 2: Content Plan + (empty or combine dates)
       - Use consistent label styling: `text-[11px] font-medium uppercase tracking-wider text-muted-foreground`
       - Use consistent value styling: `text-sm text-foreground` (font-medium for Assigned To)
       - Combine Due Date & Deadline into one row with two columns, clearly labeled
       - Add subtle separator line (`border-t border-border pt-3 mt-3`) before dates section
       - Keep the "Open task detail" link at the bottom with proper spacing

    3. **Visual Hierarchy:**
       - Group related fields visually (dates together)
       - Align all labels to the left, values below or inline
       - Use consistent icon sizing (h-3.5 w-3.5) with muted color
       - Add `tabular-nums` to all date values for alignment

    **Pattern to follow:**
    ```tsx
    <div className="grid grid-cols-2 gap-x-4 gap-y-2">
      {/* Field */}
      <div className="flex items-start gap-2">
        <Icon className="h-3.5 w-3.5 text-muted-foreground mt-0.5 flex-shrink-0" />
        <div>
          <p className="text-[11px] font-medium uppercase tracking-wider text-muted-foreground">Label</p>
          <p className="text-sm text-foreground tabular-nums">Value</p>
        </div>
      </div>
    </div>
    ```

    **Do NOT change:**
    - Any existing imports or type definitions
    - Helper functions (formatOptionalDate, getMonthLabel, isOverdue logic)
    - Any props or component signatures
    - The LABELS references
    - The "Open task detail" Link behavior
  </action>
  <verify>
    <automated>npm run build 2>&1 | head -50</automated>
  </verify>
  <done>
    - TaskDetailPanel renders without errors
    - Build passes successfully
    - Layout uses 2-column grid for compact organization
    - Status badge is properly positioned in header
    - All fields are aligned and scannable
  </done>
</task>

</tasks>

<verification>
- Component builds without TypeScript errors
- No layout shifts or broken styling
- All existing functionality preserved (links, formatting, overdue detection)
</verification>

<success_criteria>
- Task detail panel displays in a compact, organized 2-column layout
- Status badge is positioned consistently with the header
- Label/value pairs have clear alignment and visual hierarchy
- Vertical space usage is minimized without sacrificing readability
- Build completes successfully
</success_criteria>

<output>
After completion, create `.planning/quick/260405-k9m-fix-task-view-dropdown-ui-ux-improve-lay/260405-k9m-SUMMARY.md`
</output>
