# Quick Task 260405-kb0: Fix mobile hamburger, add notification bell, add My Tasks dashboard section

## must_haves

### truths
- Admin mobile header (`app/admin/layout.tsx`) shows hamburger button on the right, mobile nav opens from left side (correct orientation)
- Team mobile header (`app/team/layout.tsx`) shows hamburger button on the right, mobile nav opens from right side (awkward — same side as button)
- Dashboard notifications (`DashboardNotifications` in `dashboard-inner.tsx`) is an expandable section rendered at bottom of admin dashboard
- Notifications table has `team_member_id` column — notifications belong to team members; admin fetches all (no team_member_id filter in admin page)
- Team members already see their assigned tasks via `task_assignments` table on their dashboard
- `shadcn Popover` and `DropdownMenu` are already installed in `components/ui/`
- The `TeamMobileNav` component uses Sheet opening from `side="right"` — same side as button placement

### artifacts (current state)
- `app/admin/mobile-nav.tsx` — admin mobile nav, Sheet opens from `side="left"` (good)
- `app/team/mobile-nav.tsx` — team mobile nav, Sheet opens from `side="right"` (bad)
- `app/admin/layout.tsx` — admin header with MobileNav on right
- `app/team/layout.tsx` — team header with TeamMobileNav on right, no notification icon
- `components/admin/dashboard-inner.tsx` — DashboardNotifications, DashboardCalendar, DashboardTaskSections, DashboardMetrics
- `app/admin/page.tsx` — server component fetching notifications + calendar tasks + metrics
- `app/team/page.tsx` — team dashboard showing assigned tasks

### key_links
- Notifications actions in `app/admin/notifications/actions.ts` — markNotificationAsRead, markAllNotificationsAsRead (admin only)
- No team notification actions exist yet

## Plan

### Task 1: Fix team hamburger button and add notification icons to mobile headers

**Files:** `app/team/mobile-nav.tsx`, `app/admin/mobile-nav.tsx`, `app/team/layout.tsx`, `app/admin/layout.tsx`

**Action:**
1. **Team mobile nav**: Change `side="right"` to `side="left"` in `SheetContent` so the menu slides in from the LEFT (away from the hamburger button on the right). This makes the UX natural — button on right, panel from left.
2. **Add notification bell to both mobile headers** (admin and team):
   - Add a `Bell` icon button (from `lucide-react`) with an unread badge to the admin mobile header (next to the hamburger).
   - Add a `Bell` icon button to the team mobile header (next to the hamburger). Fetch unread notifications count in team layout (notifications can be created for team members in the `notifications` table by their `team_member_id`).
   - The bell opens a dropdown (using shadcn Popover) showing recent notifications with mark-as-read — keep it simple, similar to the existing notification list style but in a flyout.
   - On desktop, add the bell icon to the right side of the sidebar or top of the sidebar area. Specifically, add it to the top-right corner of the desktop header areas in both admin and team layouts (the `lg:hidden` header only shows on mobile, but we need to show a bell on desktop too). We'll add it inside the main header area at the top of the sidebar or as a floating element. Actually, for desktop, the sidebar takes the full left side. We'll add the bell in a small header bar above the content area at the top-right (visible on lg+ screens).

**Files to modify:**
- `app/team/mobile-nav.tsx` — change Sheet side to "left"
- `app/admin/mobile-nav.tsx` — add Bell notification button next to Menu button (mobile)
- `app/team/layout.tsx` — add Bell notification button to mobile header + desktop top bar
- `app/admin/layout.tsx` — add Bell notification button to mobile header + desktop top bar
- New file: `components/admin/notification-bell.tsx` — reusable notification Popover component (for admin mobile header)
- New file: `components/team/notification-bell.tsx` — reusable notification Popover component (for team mobile header)
- New file: `app/team/notifications/actions.ts` — server action for team members to mark their notifications as read

**Verify:**
- Team mobile nav sheet opens from left when hamburger is on right
- Bell icon visible on both admin and team mobile headers with unread badge when there are unread notifications
- Bell icon visible on both admin and team desktop views (top-right corner of content area)
- Clicking bell opens a dropdown/popover showing recent notifications
- Clicking a notification marks it as read

### Task 2: Remove NotificationsSection from dashboard and add My Tasks section

**Files:** `app/admin/page.tsx`, `components/admin/dashboard-inner.tsx`

**Action:**
1. **Remove `DashboardNotifications`** from admin dashboard (`app/admin/page.tsx`) — no longer render the full notifications section on the dashboard since we now have the bell icon. The notifications page at `/admin/notifications` remains for full list view.
2. **Create `DashboardMyTasks` section** in `dashboard-inner.tsx` — a new component that:
   - Shows tasks assigned to the current user (admin = all users' assignments, filtered to show the admin's own tasks; actually re-reading the codebase, admins don't appear in `task_assignments` table as assignees — the table only links tasks to team_members. So "My Tasks" for admin needs a different approach)
   - For **admin dashboard**: Fetch tasks that have assignments (i.e., tasks assigned to team members that the admin is managing). Actually — the user said "tasks assigned to the user — both team members and admins". Admins don't have entries in task_assignments. The correct interpretation: admins see a summary of tasks assigned to team members. Team members see their own assigned tasks via `task_assignments` which they already see in `app/team/page.tsx`.
   - For the **admin "My Tasks" section**: Show "Tasks Being Worked On" — tasks that have been assigned to team members, grouped by status (in progress, todo). Each task links to its full task page.
   - For the **team "My Tasks" section**: Use the same data as their dashboard already shows — their assigned tasks via `task_assignments`.
   
   Actually, re-reading the dashboard page more carefully — `app/admin/page.tsx` doesn't have a "My Tasks" concept for administrators at all currently. There's a `DashboardTaskSections` component showing overdue and today's tasks (global tasks, not per-user). The user wants a "My Tasks" section that shows each user their assigned tasks.
   
   For team members: they already have their assigned tasks list in their dashboard. But we should add a compact "My Tasks" section similar to the admin dashboard sections.
   
   For admin: we should show admins a "My Tasks" section. Since admins don't appear in `task_assignments`, I think the best interpretation for "My Tasks" for admin is the existing `DashboardTaskSections` (overdue, today, upcoming tasks) which is already global. The user likely just wants it to be more prominent and renamed to "My Tasks" feel.
   
   Actually, the simplest and cleanest interpretation: the user wants both admin and team to have a focused "My Tasks" section above/below the calendar that pulls from `task_assignments` for the current user. For admin, we can show tasks assigned to any team member (since admin manages them). For team, we show their own assigned tasks.
   
3. **Add `DashboardMyTasks` component** in `dashboard-inner.tsx`:
   - Accept tasks data (array of tasks with id, title, status, project info, assignee info)
   - Display in a bordered section with header "My Tasks"
   - Show grouped by status: "In Progress", "To Do", "Overdue" — with task name, project link, and status
   - Each task clicks through to the full task page using the existing `renderTaskLink` pattern from `DashboardTaskSections`
   - Limited to ~5 per group to keep it compact
4. **Wire up data** in `app/admin/page.tsx` to fetch task assignments (tasks assigned to team members) and pass to `DashboardMyTasks`
5. **Wire up data** in `app/team/page.tsx` — the team dashboard already shows assigned tasks. Keep the existing format but possibly add a more compact "My Tasks" overview.
6. **Place "My Tasks" section** above the calendar on admin dashboard.

**Verify:**
- Notifications section is removed from the dashboard page
- "My Tasks" section appears on admin dashboard (above calendar)  
- "My Tasks" section appears on team dashboard
- Tasks display with title, project, status, and are clickable to edit page
- Clicking a task navigates to the parent task page where it can be edited

### Task 3: Team notification support

**Files:** `app/team/notifications/actions.ts`, `app/team/team-db-types.ts` (or inline types)

**Action:**
1. Create `app/team/notifications/actions.ts` with `markNotificationAsRead` for team members (scoped to `team_member_id`)
2. Team notification bell fetches unread count for `team_member_id` in the team layout

**Verify:**
- Team member can mark their own notifications as read
- Team member cannot mark other members' notifications as read (RLS policy already handles this)
- Unread count updates after marking as read
